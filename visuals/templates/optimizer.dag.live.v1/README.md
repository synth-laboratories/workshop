# optimizer.dag.live.v1

Visual-first DAG workspace for a local experiment graph. Binds slot **`optimizer_run`**. Expect `algorithmId` `dag` (or `dag.*`). Consumes `optimizer_event.v1` node/partition events. No env frames.
