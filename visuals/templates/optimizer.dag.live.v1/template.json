{
  "schemaVersion": "synth.visual-template.v1",
  "id": "optimizer.dag.live.v1",
  "title": "DAG live",
  "genre": "optimizer",
  "version": "1.0.0",
  "description": "Local experiment DAG: node stages, partition progress, and honest known spend. No Craftax env frames.",
  "accent": "#F05F22",
  "shell": "shell.tsx",
  "tags": ["optimizer", "dag", "live"],
  "slots": [
    {
      "name": "optimizer_run",
      "description": "Optimizer run + optimizer_event.v1 DAG node/partition events",
      "accepts": ["optimizer_run", "fixture", "inline"],
      "required": true,
      "schema": "optimizer_event.v1"
    }
  ]
}
