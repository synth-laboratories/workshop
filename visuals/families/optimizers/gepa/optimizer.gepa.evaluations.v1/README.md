# optimizer.gepa.evaluations.v1

Child `resource-ref` → Containers `stream_id` + `/reward` URL. Binds slot **`optimizer_run`**. Does not flatten NEV or env frames into optimizer events. Missing reward is —.
