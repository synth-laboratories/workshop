{
  "schemaVersion": "synth.visual-template.v1",
  "id": "optimizer.sft.lineage.v1",
  "title": "SFT lineage",
  "genre": "optimizer",
  "version": "1.0.0",
  "description": "Base model → adapter → deployable checkpoint.",
  "accent": "#F05F22",
  "shell": "shell.tsx",
  "tags": ["optimizer", "sft", "lineage"],
  "slots": [
    {
      "name": "optimizer_run",
      "description": "Optimizer run + materialized model lineage",
      "accepts": ["optimizer_run", "fixture", "inline"],
      "required": true,
      "schema": "optimizer_event.v1"
    }
  ]
}
