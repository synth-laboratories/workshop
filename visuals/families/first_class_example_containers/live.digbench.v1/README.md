# live.digbench.v1

dig.bench live eval. Bind slot **`stream`**. Text observation, legal actions, lives/level/steps. No frames, no reward.txt, no dungeon art. Reward is shown only when the dig.bench producer/profile emits an authoritative `reward_signal` or terminal reward; status alone leaves it —.
