interface:
  display_name: "Use Human Annotations"
  short_description: "Collect durable structured or audio human review"
  default_prompt: "Use $use-human-annotations to prepare a digest-bound review task, open it in Workshop's right panel, and return the sealed result."
