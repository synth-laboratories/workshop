---
name: run-banking77-gepa
description: Build, register, admit, run, monitor, and hand off a five-seed Banking77 GEPA campaign through the current isolated Workshop instance. Use for the Banking77 GEPA proof under evals/temp/banking77-gepa; do not use for shipped recipes or another Workshop instance.
---

# Run Banking77 GEPA

Operate only through the current Workshop instance and keep all implementation in:

```text
/Users/joshuapurtell/GitHub/evals/temp/banking77-gepa/
```

Before mutating anything, confirm the runtime manifest identifies instance `l`, the window/display identity is L, and app-server `account/read` reports `type: "chatgpt"`. Stop if any of these checks fail. Never use an API provider as a fallback for the Workshop coding agent.

Load `$use-synth-containers` for container creation, discovery, registration, lifecycle, and Trace V5 evidence. Load `$use-synth-optimizers` and its GEPA reference for recipe admission, campaign execution, event reconciliation, terminal settlement, and optimizer visuals. Author a source-declared workspace recipe; never name or depend on a shipped Banking77 recipe ID.

Keep the instance boundary strict:

- Register the container only in L and use only L-owned registrations, attachments, logs, database records, visuals, traces, and receipts.
- Never start, stop, reconfigure, register into, or collect evidence from J, T, K, or another Workshop instance.
- Ensure every screenshot, visual title, trace annotation, and runtime receipt visibly identifies L.
- Treat `/Users/joshuapurtell/GitHub/evals` as the approved project source, but write implementation only beneath the target directory above.

Use Workshop's ephemeral secrets proxy for GEPA task-model and reflection-model calls. Never invoke the Keychain-backed Secrets registry, never import credentials into it, and never read, write, configure, or recover credentials from macOS Keychain. Do not expose secret values in prompts, logs, traces, screenshots, commands, Compose files, or artifacts. Revoke every bounded capability at terminal settlement and retain the revocation receipt.

Submit exactly one final five-seed GEPA campaign after the container, source declaration, dry validation, and credential route are ready. Do not start a replacement campaign after admission. Follow the admitted campaign until Workshop reports a genuine terminal state; empty watcher batches, a healthy container, or a completed worker with unsettled receipts are not terminal proof.

At settlement, reconcile the five seeds and report honest missing evidence as unavailable. The final handoff must include:

```text
Workshop instance: L
L data root
L Codex home
L authentication state
container ID and endpoint
project-source approval
installed skill path
campaign and optimizer IDs
visual and trace IDs
credential revocation
```
