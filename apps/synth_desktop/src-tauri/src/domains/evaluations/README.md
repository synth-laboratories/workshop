# Evaluation domain

Owns admission mapping onto `FailureKind::Admission` (and container-coded refusals), plus terminal child reconciliation to `not_started`.

Does **not** own: failure identity, logs, or renderer presentation.
