//! Container domain facts. Policy lives in `definition`; settlement lives here.

pub use crate::platform::failure::definition::{
    ContainerFailure, HealthObservation, HealthSource, HealthStatus,
};
