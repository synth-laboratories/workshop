# Container domain

Owns container health, launch, capability, and probe meaning. Settles `current_failure_id` on `containers`.

Does **not** own: failure identity, logs, renderer presentation, or evaluation admission codes that are not container-coded.
