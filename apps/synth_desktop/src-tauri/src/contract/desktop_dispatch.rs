// @generated by scripts/generate-workshop-dispatch.py; do not edit.
use anyhow::Context;
use serde_json::{json, Value};
use tauri::Manager;
pub const NAMES: &[&str] = &[
    "desktop_state_get",
    "desktop_state_commit",
    "agent_backends_list",
    "agent_sessions_list",
    "agent_session_start",
    "agent_session_send",
    "agent_session_cancel",
    "agent_session_close",
    "agent_session_resume",
    "desktop_instance_diagnostics",
    "desktop_instances_list",
    "desktop_image_preview",
    "core_diagnostics",
    "runtime_contracts",
    "core_events_after",
    "core_session_events_after",
    "core_session_events_tail",
    "core_session_events_before",
    "intern_sessions_list",
    "intern_session_create",
    "intern_session_send",
    "intern_session_control",
    "intern_session_events_after",
    "data_containers_list",
    "data_containers_get",
    "data_containers_register",
    "data_containers_probe",
    "data_containers_reconcile",
    "data_containers_restart",
    "data_trace_research_request",
    "data_traces_list",
    "data_traces_get",
    "data_trace_materialize",
    "data_traces_ingest",
    "data_trace_projection_resolve",
    "analysis_projection_get",
    "analysis_findings_list",
    "analysis_campaigns_list",
    "analysis_review_record",
    "data_usage_list",
    "model_performance_summary",
    "model_performance_turn_samples",
    "usage_summary",
    "tariff_catalog",
    "update_status",
    "update_open_download",
    "data_counts",
    "optimizers_algorithms_list",
    "optimizers_recipes_list",
    "optimizers_recipe_start",
    "optimizers_stage_eval_candidates",
    "optimizers_list",
    "optimizers_get",
    "optimizers_run_view",
    "optimizers_run_view_v2",
    "optimizers_evidence_page",
    "optimizers_run_summary",
    "optimizers_run_collection",
    "optimizers_run_collection_item",
    "optimizers_projection_at",
    "optimizers_visual_render_receipt",
    "optimizers_create",
    "optimizers_refresh",
    "optimizers_events_after",
    "optimizers_artifacts_list",
    "optimizers_artifact_read_range",
    "optimizers_frames_latest",
    "optimizers_frames_list",
    "optimizers_frame_content",
    "optimizers_get_state",
    "optimizers_get_state_batch",
    "optimizers_relationships",
    "optimizers_cancel",
    "optimizers_pause",
    "optimizers_resume",
    "optimizers_open_visual",
    "optimizers_import_local",
    "optimizers_reconcile_cloud",
    "optimizers_list_cloud",
    "optimizers_saved_loras_search",
    "optimizers_run_checkpoints_list",
    "optimizers_run_outputs",
    "optimizers_training_models",
    "optimizers_saved_lora_archive",
    "optimizers_saved_lora_download",
    "optimizers_saved_lora_import",
    "optimizers_checkpoint_infer",
    "optimizers_saved_lora_patch",
    "optimizers_saved_lora_publish",
    "optimizers_training_reconcile",
    "optimizers_container_experiment_action",
    "plugins_status",
    "plugins_list",
    "plugins_manage",
    "plugins_set_release_channel",
    "jesterky_analysis_settings",
    "computer_use_status",
    "computer_use_install",
    "computer_use_remove",
    "computer_use_revoke_app",
    "computer_use_open_settings",
    "browser_runtime_status",
    "browser_policy_allow_origin",
    "browser_policy_revoke_origin",
    "visual_subscription_ready",
    "visual_stream_poll",
    "visual_media_read",
    "diagnostics_report",
    "diagnostics_status",
    "diagnostics_query",
    "diagnostics_explain",
    "diagnostics_bundle",
    "diagnostics_clear_index",
    "optimizer_sidecar_status",
    "optimizer_sidecar_install",
    "optimizer_sidecar_start",
    "optimizer_sidecar_stop",
    "optimizer_sidecar_version",
    "optimizer_sidecar_uninstall",
    "visuals_templates_list",
    "visuals_templates_get",
    "visuals_list",
    "visuals_get",
    "visuals_engine",
    "visuals_presentation_get",
    "visuals_presentation_put",
    "visuals_snapshots_list",
    "visuals_snapshot_put",
    "visuals_recordings_list",
    "visuals_recording_put",
    "visuals_observation_report",
    "visuals_revisions",
    "visuals_annotations_list",
    "visuals_annotation_create",
    "visuals_seals_list",
    "visuals_seal",
    "visuals_seal_get",
    "visuals_upload_status",
    "visuals_share_seal",
    "visuals_open_shared",
    "visuals_create",
    "visuals_update",
    "visuals_save",
    "visuals_fork",
    "visuals_archive",
    "visuals_show",
    "visuals_content",
    "visuals_renditions",
    "visuals_rendition",
    "visuals_render",
    "human_annotation_create",
    "human_annotation_preview",
    "human_annotation_session_open",
    "human_annotation_show",
    "human_annotation_answer_set",
    "human_annotation_answer_clear",
    "human_annotation_comment_create",
    "human_annotation_audio_begin",
    "human_annotation_audio_append",
    "human_annotation_audio_finish",
    "human_annotation_audio_read",
    "human_annotation_audio_transcribe",
    "human_annotation_transcript_correct",
    "human_annotation_submit",
    "human_annotation_list",
    "human_annotation_status",
    "human_annotation_cancel",
    "human_annotation_export",
    "human_annotation_campaign_create",
    "human_annotation_campaign_status",
    "human_annotation_campaign_close",
    "human_annotation_campaign_adjudicate",
    "human_annotation_supersede",
    "reports_list",
    "reports_get",
    "reports_revision_get",
    "reports_validate",
    "reports_pin_all",
    "reports_create",
    "reports_update",
    "reports_archive",
    "reports_restore",
    "reports_visibility_requests",
    "reports_visibility_request",
    "reports_visibility_decide",
    "reports_seal",
    "reports_seals_list",
    "reports_seal_get",
    "reports_seals_compare",
    "reports_experiments_list",
    "reports_experiment_upsert",
    "experiments_list",
    "experiments_get",
    "experiments_attach_evidence",
    "experiments_create",
    "experiments_create_child",
    "experiments_relate",
    "experiments_activate",
    "experiments_update",
    "experiments_finalize",
    "research_log_list",
    "research_log_append",
    "reports_log_list",
    "reports_log_append",
    "reports_upload_status",
    "reports_share",
    "reports_audience_set",
    "reports_audience_revoke",
    "reports_promote",
    "reports_open_shared",
    "reports_comments_list",
    "reports_comment_create",
    "synth_config_get",
    "model_catalog_get",
    "model_catalog_refresh",
    "synth_config_update",
    "model_performance_get",
    "account_begin_sign_in",
    "account_get_summary",
    "account_refresh",
    "account_open_billing",
    "account_poll_sign_in",
    "account_cancel_sign_in",
    "account_sign_out",
    "codex_oauth_begin",
    "codex_oauth_complete_manual",
    "codex_oauth_ensure_ready",
    "codex_oauth_status",
    "codex_oauth_disconnect",
    "codex_oauth_cancel",
    "model_multi_agent_list",
    "model_multi_agent_update",
    "workspace_access_get",
    "workspace_access_update",
    "desktop_permissions_get",
    "desktop_permissions_update",
    "workspace_scope_get",
    "workspace_scope_choose_and_attach",
    "workspace_scope_recent_folders",
    "workspace_scope_attach_recent",
    "workspace_scope_remove_attachment",
    "workspace_scope_request_agent_grant",
    "workspace_scope_grants_list",
    "workspace_scope_approve_request",
    "workspace_scope_deny_request",
    "migration_scan",
    "migration_prepare",
    "migration_apply",
    "migration_cancel",
    "laguna_get_status",
    "laguna_reload",
    "laguna_register_policy",
    "laguna_policies",
    "laguna_adapter_status",
    "laguna_adapter_download",
    "laguna_models_list",
    "laguna_models_set_directory",
    "laguna_models_clear_directory",
    "laguna_inference_snapshot",
    "laguna_inference_stream_start",
    "laguna_inference_stream_stop",
    "laguna_model_unload",
    "laguna_model_download",
    "laguna_model_delete",
    "laguna_settings_snapshot",
    "laguna_settings_update",
    "training_models_list",
    "training_models_download",
    "training_models_delete",
    "training_mlx_runtime_status",
    "training_mlx_runtime_install",
    "training_artifacts_list",
    "training_artifacts_get",
    "training_artifacts_export",
    "training_artifacts_delete",
    "training_artifacts_launch_inference",
    "whisper_models_list",
    "whisper_model_download",
    "whisper_models_set_selected",
    "whisper_models_clear",
    "whisper_runtime_status",
    "whisper_runtime_warm",
    "whisper_transcribe",
    "whisper_transcribe_base64",
    "skills_list",
    "context_snapshot",
    "context_workspace_agents_update",
    "context_skill_update",
    "context_mcp_group_update",
    "context_cookbooks_install",
    "context_cookbooks_cancel",
    "context_cookbooks_set_enabled",
    "context_cookbooks_uninstall",
    "workspace_choose_directory",
    "codex_session_start",
    "codex_turn_start",
    "codex_turn_send",
    "synth_cloud_inference_status",
    "codex_turn_interrupt",
    "codex_thread_compact",
    "codex_thread_read",
    "codex_thread_items_list",
    "codex_turn_steer",
    "codex_approval_resolve",
    "codex_session_close",
    "codex_sessions_list",
    "codex_default_workspace",
    "terminal_create",
    "terminal_list",
    "terminal_snapshot",
    "terminal_write",
    "terminal_resize",
    "terminal_ghostty_mount",
    "terminal_ghostty_set_frame",
    "terminal_ghostty_set_visible",
    "terminal_ghostty_focus",
    "terminal_ghostty_unmount",
    "terminal_close",
    "secrets_workspace_roots_list",
    "secrets_bindings_list",
    "secrets_locators_list",
    "secrets_locator_remember_external",
    "secrets_locator_register",
    "secrets_locator_forget",
    "secrets_list",
    "secrets_create",
    "secrets_replace",
    "secrets_delete",
    "secrets_test",
    "secrets_request_use",
    "secrets_grant_use",
    "secrets_deny_use",
    "secrets_capabilities_list",
    "secrets_revoke_capability",
    "secrets_request_env_import",
    "secrets_commit_env_import",
    "secrets_audit_list",
    "secrets_proxy_status",
    "secrets_pending",
    "secrets_deny_env_import",
    "product_telemetry_get_policy",
    "product_telemetry_set_opt_out",
    "failures_query",
    "failures_get",
    "failures_timeline",
    "logs_query",
    "failure_export_bundle",
    "observability_status",
    "product_telemetry_set_consent",
    "product_telemetry_recent",
    "product_telemetry_flush_now",
    "workspace_read_file",
    "workspace_list_dir",
    "document_show",
    "visuals_template_shell_source",
    "visuals_template_save",
    "visuals_template_create",
    "visuals_template_validate",
    "approvals_pending",
    "approvals_approve_digest",
    "project_sources_get",
    "project_sources_refresh",
    "project_source_add",
    "project_source_remove",
];

type Reply<'a> = std::pin::Pin<Box<dyn std::future::Future<Output = anyhow::Result<Value>> + Send + 'a>>;
pub fn invoke<'a>(app: &'a tauri::AppHandle, name: &str, args: Value) -> Reply<'a> {
    match name {
        "desktop_state_get" => operation_0(app, args),
        "desktop_state_commit" => operation_1(app, args),
        "agent_backends_list" => operation_2(app, args),
        "agent_sessions_list" => operation_3(app, args),
        "agent_session_start" => operation_4(app, args),
        "agent_session_send" => operation_5(app, args),
        "agent_session_cancel" => operation_6(app, args),
        "agent_session_close" => operation_7(app, args),
        "agent_session_resume" => operation_8(app, args),
        "desktop_instance_diagnostics" => operation_9(app, args),
        "desktop_instances_list" => operation_10(app, args),
        "desktop_image_preview" => operation_11(app, args),
        "core_diagnostics" => operation_12(app, args),
        "runtime_contracts" => operation_13(app, args),
        "core_events_after" => operation_14(app, args),
        "core_session_events_after" => operation_15(app, args),
        "core_session_events_tail" => operation_16(app, args),
        "core_session_events_before" => operation_17(app, args),
        "intern_sessions_list" => operation_18(app, args),
        "intern_session_create" => operation_19(app, args),
        "intern_session_send" => operation_20(app, args),
        "intern_session_control" => operation_21(app, args),
        "intern_session_events_after" => operation_22(app, args),
        "data_containers_list" => operation_23(app, args),
        "data_containers_get" => operation_24(app, args),
        "data_containers_register" => operation_25(app, args),
        "data_containers_probe" => operation_26(app, args),
        "data_containers_reconcile" => operation_27(app, args),
        "data_containers_restart" => operation_28(app, args),
        "data_trace_research_request" => operation_29(app, args),
        "data_traces_list" => operation_30(app, args),
        "data_traces_get" => operation_31(app, args),
        "data_trace_materialize" => operation_32(app, args),
        "data_traces_ingest" => operation_33(app, args),
        "data_trace_projection_resolve" => operation_34(app, args),
        "analysis_projection_get" => operation_35(app, args),
        "analysis_findings_list" => operation_36(app, args),
        "analysis_campaigns_list" => operation_37(app, args),
        "analysis_review_record" => operation_38(app, args),
        "data_usage_list" => operation_39(app, args),
        "model_performance_summary" => operation_40(app, args),
        "model_performance_turn_samples" => operation_41(app, args),
        "usage_summary" => operation_42(app, args),
        "tariff_catalog" => operation_43(app, args),
        "update_status" => operation_44(app, args),
        "update_open_download" => operation_45(app, args),
        "data_counts" => operation_46(app, args),
        "optimizers_algorithms_list" => operation_47(app, args),
        "optimizers_recipes_list" => operation_48(app, args),
        "optimizers_recipe_start" => operation_49(app, args),
        "optimizers_stage_eval_candidates" => operation_50(app, args),
        "optimizers_list" => operation_51(app, args),
        "optimizers_get" => operation_52(app, args),
        "optimizers_run_view" => operation_53(app, args),
        "optimizers_run_view_v2" => operation_54(app, args),
        "optimizers_evidence_page" => operation_55(app, args),
        "optimizers_run_summary" => operation_56(app, args),
        "optimizers_run_collection" => operation_57(app, args),
        "optimizers_run_collection_item" => operation_58(app, args),
        "optimizers_projection_at" => operation_59(app, args),
        "optimizers_visual_render_receipt" => operation_60(app, args),
        "optimizers_create" => operation_61(app, args),
        "optimizers_refresh" => operation_62(app, args),
        "optimizers_events_after" => operation_63(app, args),
        "optimizers_artifacts_list" => operation_64(app, args),
        "optimizers_artifact_read_range" => operation_65(app, args),
        "optimizers_frames_latest" => operation_66(app, args),
        "optimizers_frames_list" => operation_67(app, args),
        "optimizers_frame_content" => operation_68(app, args),
        "optimizers_get_state" => operation_69(app, args),
        "optimizers_get_state_batch" => operation_70(app, args),
        "optimizers_relationships" => operation_71(app, args),
        "optimizers_cancel" => operation_72(app, args),
        "optimizers_pause" => operation_73(app, args),
        "optimizers_resume" => operation_74(app, args),
        "optimizers_open_visual" => operation_75(app, args),
        "optimizers_import_local" => operation_76(app, args),
        "optimizers_reconcile_cloud" => operation_77(app, args),
        "optimizers_list_cloud" => operation_78(app, args),
        "optimizers_saved_loras_search" => operation_79(app, args),
        "optimizers_run_checkpoints_list" => operation_80(app, args),
        "optimizers_run_outputs" => operation_81(app, args),
        "optimizers_training_models" => operation_82(app, args),
        "optimizers_saved_lora_archive" => operation_83(app, args),
        "optimizers_saved_lora_download" => operation_84(app, args),
        "optimizers_saved_lora_import" => operation_85(app, args),
        "optimizers_checkpoint_infer" => operation_86(app, args),
        "optimizers_saved_lora_patch" => operation_87(app, args),
        "optimizers_saved_lora_publish" => operation_88(app, args),
        "optimizers_training_reconcile" => operation_89(app, args),
        "optimizers_container_experiment_action" => operation_90(app, args),
        "plugins_status" => operation_91(app, args),
        "plugins_list" => operation_92(app, args),
        "plugins_manage" => operation_93(app, args),
        "plugins_set_release_channel" => operation_94(app, args),
        "jesterky_analysis_settings" => operation_95(app, args),
        "computer_use_status" => operation_96(app, args),
        "computer_use_install" => operation_97(app, args),
        "computer_use_remove" => operation_98(app, args),
        "computer_use_revoke_app" => operation_99(app, args),
        "computer_use_open_settings" => operation_100(app, args),
        "browser_runtime_status" => operation_101(app, args),
        "browser_policy_allow_origin" => operation_102(app, args),
        "browser_policy_revoke_origin" => operation_103(app, args),
        "visual_subscription_ready" => operation_104(app, args),
        "visual_stream_poll" => operation_105(app, args),
        "visual_media_read" => operation_106(app, args),
        "diagnostics_report" => operation_107(app, args),
        "diagnostics_status" => operation_108(app, args),
        "diagnostics_query" => operation_109(app, args),
        "diagnostics_explain" => operation_110(app, args),
        "diagnostics_bundle" => operation_111(app, args),
        "diagnostics_clear_index" => operation_112(app, args),
        "optimizer_sidecar_status" => operation_113(app, args),
        "optimizer_sidecar_install" => operation_114(app, args),
        "optimizer_sidecar_start" => operation_115(app, args),
        "optimizer_sidecar_stop" => operation_116(app, args),
        "optimizer_sidecar_version" => operation_117(app, args),
        "optimizer_sidecar_uninstall" => operation_118(app, args),
        "visuals_templates_list" => operation_119(app, args),
        "visuals_templates_get" => operation_120(app, args),
        "visuals_list" => operation_121(app, args),
        "visuals_get" => operation_122(app, args),
        "visuals_engine" => operation_123(app, args),
        "visuals_presentation_get" => operation_124(app, args),
        "visuals_presentation_put" => operation_125(app, args),
        "visuals_snapshots_list" => operation_126(app, args),
        "visuals_snapshot_put" => operation_127(app, args),
        "visuals_recordings_list" => operation_128(app, args),
        "visuals_recording_put" => operation_129(app, args),
        "visuals_observation_report" => operation_130(app, args),
        "visuals_revisions" => operation_131(app, args),
        "visuals_annotations_list" => operation_132(app, args),
        "visuals_annotation_create" => operation_133(app, args),
        "visuals_seals_list" => operation_134(app, args),
        "visuals_seal" => operation_135(app, args),
        "visuals_seal_get" => operation_136(app, args),
        "visuals_upload_status" => operation_137(app, args),
        "visuals_share_seal" => operation_138(app, args),
        "visuals_open_shared" => operation_139(app, args),
        "visuals_create" => operation_140(app, args),
        "visuals_update" => operation_141(app, args),
        "visuals_save" => operation_142(app, args),
        "visuals_fork" => operation_143(app, args),
        "visuals_archive" => operation_144(app, args),
        "visuals_show" => operation_145(app, args),
        "visuals_content" => operation_146(app, args),
        "visuals_renditions" => operation_147(app, args),
        "visuals_rendition" => operation_148(app, args),
        "visuals_render" => operation_149(app, args),
        "human_annotation_create" => operation_150(app, args),
        "human_annotation_preview" => operation_151(app, args),
        "human_annotation_session_open" => operation_152(app, args),
        "human_annotation_show" => operation_153(app, args),
        "human_annotation_answer_set" => operation_154(app, args),
        "human_annotation_answer_clear" => operation_155(app, args),
        "human_annotation_comment_create" => operation_156(app, args),
        "human_annotation_audio_begin" => operation_157(app, args),
        "human_annotation_audio_append" => operation_158(app, args),
        "human_annotation_audio_finish" => operation_159(app, args),
        "human_annotation_audio_read" => operation_160(app, args),
        "human_annotation_audio_transcribe" => operation_161(app, args),
        "human_annotation_transcript_correct" => operation_162(app, args),
        "human_annotation_submit" => operation_163(app, args),
        "human_annotation_list" => operation_164(app, args),
        "human_annotation_status" => operation_165(app, args),
        "human_annotation_cancel" => operation_166(app, args),
        "human_annotation_export" => operation_167(app, args),
        "human_annotation_campaign_create" => operation_168(app, args),
        "human_annotation_campaign_status" => operation_169(app, args),
        "human_annotation_campaign_close" => operation_170(app, args),
        "human_annotation_campaign_adjudicate" => operation_171(app, args),
        "human_annotation_supersede" => operation_172(app, args),
        "reports_list" => operation_173(app, args),
        "reports_get" => operation_174(app, args),
        "reports_revision_get" => operation_175(app, args),
        "reports_validate" => operation_176(app, args),
        "reports_pin_all" => operation_177(app, args),
        "reports_create" => operation_178(app, args),
        "reports_update" => operation_179(app, args),
        "reports_archive" => operation_180(app, args),
        "reports_restore" => operation_181(app, args),
        "reports_visibility_requests" => operation_182(app, args),
        "reports_visibility_request" => operation_183(app, args),
        "reports_visibility_decide" => operation_184(app, args),
        "reports_seal" => operation_185(app, args),
        "reports_seals_list" => operation_186(app, args),
        "reports_seal_get" => operation_187(app, args),
        "reports_seals_compare" => operation_188(app, args),
        "reports_experiments_list" => operation_189(app, args),
        "reports_experiment_upsert" => operation_190(app, args),
        "experiments_list" => operation_191(app, args),
        "experiments_get" => operation_192(app, args),
        "experiments_attach_evidence" => operation_193(app, args),
        "experiments_create" => operation_194(app, args),
        "experiments_create_child" => operation_195(app, args),
        "experiments_relate" => operation_196(app, args),
        "experiments_activate" => operation_197(app, args),
        "experiments_update" => operation_198(app, args),
        "experiments_finalize" => operation_199(app, args),
        "research_log_list" => operation_200(app, args),
        "research_log_append" => operation_201(app, args),
        "reports_log_list" => operation_202(app, args),
        "reports_log_append" => operation_203(app, args),
        "reports_upload_status" => operation_204(app, args),
        "reports_share" => operation_205(app, args),
        "reports_audience_set" => operation_206(app, args),
        "reports_audience_revoke" => operation_207(app, args),
        "reports_promote" => operation_208(app, args),
        "reports_open_shared" => operation_209(app, args),
        "reports_comments_list" => operation_210(app, args),
        "reports_comment_create" => operation_211(app, args),
        "synth_config_get" => operation_212(app, args),
        "model_catalog_get" => operation_213(app, args),
        "model_catalog_refresh" => operation_214(app, args),
        "synth_config_update" => operation_215(app, args),
        "model_performance_get" => operation_216(app, args),
        "account_begin_sign_in" => operation_217(app, args),
        "account_get_summary" => operation_218(app, args),
        "account_refresh" => operation_219(app, args),
        "account_open_billing" => operation_220(app, args),
        "account_poll_sign_in" => operation_221(app, args),
        "account_cancel_sign_in" => operation_222(app, args),
        "account_sign_out" => operation_223(app, args),
        "codex_oauth_begin" => operation_224(app, args),
        "codex_oauth_complete_manual" => operation_225(app, args),
        "codex_oauth_ensure_ready" => operation_226(app, args),
        "codex_oauth_status" => operation_227(app, args),
        "codex_oauth_disconnect" => operation_228(app, args),
        "codex_oauth_cancel" => operation_229(app, args),
        "model_multi_agent_list" => operation_230(app, args),
        "model_multi_agent_update" => operation_231(app, args),
        "workspace_access_get" => operation_232(app, args),
        "workspace_access_update" => operation_233(app, args),
        "desktop_permissions_get" => operation_234(app, args),
        "desktop_permissions_update" => operation_235(app, args),
        "workspace_scope_get" => operation_236(app, args),
        "workspace_scope_choose_and_attach" => operation_237(app, args),
        "workspace_scope_recent_folders" => operation_238(app, args),
        "workspace_scope_attach_recent" => operation_239(app, args),
        "workspace_scope_remove_attachment" => operation_240(app, args),
        "workspace_scope_request_agent_grant" => operation_241(app, args),
        "workspace_scope_grants_list" => operation_242(app, args),
        "workspace_scope_approve_request" => operation_243(app, args),
        "workspace_scope_deny_request" => operation_244(app, args),
        "migration_scan" => operation_245(app, args),
        "migration_prepare" => operation_246(app, args),
        "migration_apply" => operation_247(app, args),
        "migration_cancel" => operation_248(app, args),
        "laguna_get_status" => operation_249(app, args),
        "laguna_reload" => operation_250(app, args),
        "laguna_register_policy" => operation_251(app, args),
        "laguna_policies" => operation_252(app, args),
        "laguna_adapter_status" => operation_253(app, args),
        "laguna_adapter_download" => operation_254(app, args),
        "laguna_models_list" => operation_255(app, args),
        "laguna_models_set_directory" => operation_256(app, args),
        "laguna_models_clear_directory" => operation_257(app, args),
        "laguna_inference_snapshot" => operation_258(app, args),
        "laguna_inference_stream_start" => operation_259(app, args),
        "laguna_inference_stream_stop" => operation_260(app, args),
        "laguna_model_unload" => operation_261(app, args),
        "laguna_model_download" => operation_262(app, args),
        "laguna_model_delete" => operation_263(app, args),
        "laguna_settings_snapshot" => operation_264(app, args),
        "laguna_settings_update" => operation_265(app, args),
        "training_models_list" => operation_266(app, args),
        "training_models_download" => operation_267(app, args),
        "training_models_delete" => operation_268(app, args),
        "training_mlx_runtime_status" => operation_269(app, args),
        "training_mlx_runtime_install" => operation_270(app, args),
        "training_artifacts_list" => operation_271(app, args),
        "training_artifacts_get" => operation_272(app, args),
        "training_artifacts_export" => operation_273(app, args),
        "training_artifacts_delete" => operation_274(app, args),
        "training_artifacts_launch_inference" => operation_275(app, args),
        "whisper_models_list" => operation_276(app, args),
        "whisper_model_download" => operation_277(app, args),
        "whisper_models_set_selected" => operation_278(app, args),
        "whisper_models_clear" => operation_279(app, args),
        "whisper_runtime_status" => operation_280(app, args),
        "whisper_runtime_warm" => operation_281(app, args),
        "whisper_transcribe" => operation_282(app, args),
        "whisper_transcribe_base64" => operation_283(app, args),
        "skills_list" => operation_284(app, args),
        "context_snapshot" => operation_285(app, args),
        "context_workspace_agents_update" => operation_286(app, args),
        "context_skill_update" => operation_287(app, args),
        "context_mcp_group_update" => operation_288(app, args),
        "context_cookbooks_install" => operation_289(app, args),
        "context_cookbooks_cancel" => operation_290(app, args),
        "context_cookbooks_set_enabled" => operation_291(app, args),
        "context_cookbooks_uninstall" => operation_292(app, args),
        "workspace_choose_directory" => operation_293(app, args),
        "codex_session_start" => operation_294(app, args),
        "codex_turn_start" => operation_295(app, args),
        "codex_turn_send" => operation_296(app, args),
        "synth_cloud_inference_status" => operation_297(app, args),
        "codex_turn_interrupt" => operation_298(app, args),
        "codex_thread_compact" => operation_299(app, args),
        "codex_thread_read" => operation_300(app, args),
        "codex_thread_items_list" => operation_301(app, args),
        "codex_turn_steer" => operation_302(app, args),
        "codex_approval_resolve" => operation_303(app, args),
        "codex_session_close" => operation_304(app, args),
        "codex_sessions_list" => operation_305(app, args),
        "codex_default_workspace" => operation_306(app, args),
        "terminal_create" => operation_307(app, args),
        "terminal_list" => operation_308(app, args),
        "terminal_snapshot" => operation_309(app, args),
        "terminal_write" => operation_310(app, args),
        "terminal_resize" => operation_311(app, args),
        "terminal_ghostty_mount" => operation_312(app, args),
        "terminal_ghostty_set_frame" => operation_313(app, args),
        "terminal_ghostty_set_visible" => operation_314(app, args),
        "terminal_ghostty_focus" => operation_315(app, args),
        "terminal_ghostty_unmount" => operation_316(app, args),
        "terminal_close" => operation_317(app, args),
        "secrets_workspace_roots_list" => operation_318(app, args),
        "secrets_bindings_list" => operation_319(app, args),
        "secrets_locators_list" => operation_320(app, args),
        "secrets_locator_remember_external" => operation_321(app, args),
        "secrets_locator_register" => operation_322(app, args),
        "secrets_locator_forget" => operation_323(app, args),
        "secrets_list" => operation_324(app, args),
        "secrets_create" => operation_325(app, args),
        "secrets_replace" => operation_326(app, args),
        "secrets_delete" => operation_327(app, args),
        "secrets_test" => operation_328(app, args),
        "secrets_request_use" => operation_329(app, args),
        "secrets_grant_use" => operation_330(app, args),
        "secrets_deny_use" => operation_331(app, args),
        "secrets_capabilities_list" => operation_332(app, args),
        "secrets_revoke_capability" => operation_333(app, args),
        "secrets_request_env_import" => operation_334(app, args),
        "secrets_commit_env_import" => operation_335(app, args),
        "secrets_audit_list" => operation_336(app, args),
        "secrets_proxy_status" => operation_337(app, args),
        "secrets_pending" => operation_338(app, args),
        "secrets_deny_env_import" => operation_339(app, args),
        "product_telemetry_get_policy" => operation_340(app, args),
        "product_telemetry_set_opt_out" => operation_341(app, args),
        "failures_query" => operation_342(app, args),
        "failures_get" => operation_343(app, args),
        "failures_timeline" => operation_344(app, args),
        "logs_query" => operation_345(app, args),
        "failure_export_bundle" => operation_346(app, args),
        "observability_status" => operation_347(app, args),
        "product_telemetry_set_consent" => operation_348(app, args),
        "product_telemetry_recent" => operation_349(app, args),
        "product_telemetry_flush_now" => operation_350(app, args),
        "workspace_read_file" => operation_351(app, args),
        "workspace_list_dir" => operation_352(app, args),
        "document_show" => operation_353(app, args),
        "visuals_template_shell_source" => operation_354(app, args),
        "visuals_template_save" => operation_355(app, args),
        "visuals_template_create" => operation_356(app, args),
        "visuals_template_validate" => operation_357(app, args),
        "approvals_pending" => operation_358(app, args),
        "approvals_approve_digest" => operation_359(app, args),
        "project_sources_get" => operation_360(app, args),
        "project_sources_refresh" => operation_361(app, args),
        "project_source_add" => operation_362(app, args),
        "project_source_remove" => operation_363(app, args),
        _ => Box::pin(async { anyhow::bail!("unknown desktop operation") }),
    }
}

fn operation_0(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/desktop_state.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::desktop_state::desktop_state_get(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_1(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/desktop_state.rs
            let allowed: &[&str] = &["input"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::desktop_state::desktop_state_commit(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("input").cloned().unwrap_or(Value::Null)).context("invalid input")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_2(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_backends_list(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_3(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_sessions_list(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_4(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_session_start(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_5(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &["sessionId", "text"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_session_send(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("text").cloned().unwrap_or(Value::Null)).context("invalid text")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_6(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_session_cancel(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_7(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_session_close(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_8(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/acp/commands.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::acp::commands::agent_session_resume(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_9(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/contract/specta.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::contract::specta::desktop_instance_diagnostics();
            Ok(json!({"result": result}))
    })
}

fn operation_10(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/contract/specta.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::contract::specta::desktop_instances_list().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_11(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::desktop_image_preview(serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_12(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::core_diagnostics(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_13(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::runtime_contracts(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_14(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["afterSequence", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::core_events_after(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("afterSequence").cloned().unwrap_or(Value::Null)).context("invalid afterSequence")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_15(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "afterSequence", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::core_session_events_after(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("afterSequence").cloned().unwrap_or(Value::Null)).context("invalid afterSequence")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_16(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::core_session_events_tail(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_17(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "beforeSequence", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::core_session_events_before(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("beforeSequence").cloned().unwrap_or(Value::Null)).context("invalid beforeSequence")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_18(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::intern_sessions_list(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_19(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::intern_session_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_20(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::intern_session_send(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_21(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::intern_session_control(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_22(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "afterSequence", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::intern_session_events_after(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("afterSequence").cloned().unwrap_or(Value::Null)).context("invalid afterSequence")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_23(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_containers_list(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_24(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["containerId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_containers_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("containerId").cloned().unwrap_or(Value::Null)).context("invalid containerId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_25(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_containers_register(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_26(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["containerId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_containers_probe(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("containerId").cloned().unwrap_or(Value::Null)).context("invalid containerId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_27(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["containerId", "sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_containers_reconcile(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("containerId").cloned().unwrap_or(Value::Null)).context("invalid containerId")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_28(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["containerId", "sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_containers_restart(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("containerId").cloned().unwrap_or(Value::Null)).context("invalid containerId")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_29(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["operation", "argumentsJson"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_trace_research_request(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("operation").cloned().unwrap_or(Value::Null)).context("invalid operation")?, serde_json::from_value(args.get("argumentsJson").cloned().unwrap_or(Value::Null)).context("invalid argumentsJson")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_30(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_traces_list(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_31(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["traceId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_traces_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("traceId").cloned().unwrap_or(Value::Null)).context("invalid traceId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_32(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["containerId", "rolloutId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_trace_materialize(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("containerId").cloned().unwrap_or(Value::Null)).context("invalid containerId")?, serde_json::from_value(args.get("rolloutId").cloned().unwrap_or(Value::Null)).context("invalid rolloutId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_33(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_traces_ingest(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_34(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["traceDigest", "projectionKind"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_trace_projection_resolve(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("traceDigest").cloned().unwrap_or(Value::Null)).context("invalid traceDigest")?, serde_json::from_value(args.get("projectionKind").cloned().unwrap_or(Value::Null)).context("invalid projectionKind")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_35(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["kind", "digest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::analysis_projection_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("kind").cloned().unwrap_or(Value::Null)).context("invalid kind")?, serde_json::from_value(args.get("digest").cloned().unwrap_or(Value::Null)).context("invalid digest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_36(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["traceDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::analysis_findings_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("traceDigest").cloned().unwrap_or(Value::Null)).context("invalid traceDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_37(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["evalRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::analysis_campaigns_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("evalRunId").cloned().unwrap_or(Value::Null)).context("invalid evalRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_38(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["findingId", "evidenceHeadDigest", "decision", "rationale"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::analysis_review_record(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("findingId").cloned().unwrap_or(Value::Null)).context("invalid findingId")?, serde_json::from_value(args.get("evidenceHeadDigest").cloned().unwrap_or(Value::Null)).context("invalid evidenceHeadDigest")?, serde_json::from_value(args.get("decision").cloned().unwrap_or(Value::Null)).context("invalid decision")?, serde_json::from_value(args.get("rationale").cloned().unwrap_or(Value::Null)).context("invalid rationale")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_39(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_usage_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_40(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_performance_summary(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_41(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_performance_turn_samples(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_42(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["window"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::usage_summary(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("window").cloned().unwrap_or(Value::Null)).context("invalid window")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_43(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::tariff_catalog();
            Ok(json!({"result": result}))
    })
}

fn operation_44(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::update_status().await;
            Ok(json!({"result": result}))
    })
}

fn operation_45(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::update_open_download(app.clone()).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_46(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::data_counts(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_47(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_algorithms_list(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_48(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionRef"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_recipes_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionRef").cloned().unwrap_or(Value::Null)).context("invalid sessionRef")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_49(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_recipe_start(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_50(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_stage_eval_candidates(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_51(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_52(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_53(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "ifNewerThan"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_view(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("ifNewerThan").cloned().unwrap_or(Value::Null)).context("invalid ifNewerThan")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_54(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_view_v2(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_55(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "window", "held", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_evidence_page(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("window").cloned().unwrap_or(Value::Null)).context("invalid window")?, serde_json::from_value(args.get("held").cloned().unwrap_or(Value::Null)).context("invalid held")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_56(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "ifNewerThan"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_summary(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("ifNewerThan").cloned().unwrap_or(Value::Null)).context("invalid ifNewerThan")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_57(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "collection", "query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_collection(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("collection").cloned().unwrap_or(Value::Null)).context("invalid collection")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_58(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "collection", "itemId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_collection_item(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("collection").cloned().unwrap_or(Value::Null)).context("invalid collection")?, serde_json::from_value(args.get("itemId").cloned().unwrap_or(Value::Null)).context("invalid itemId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_59(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "sequence"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_projection_at(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("sequence").cloned().unwrap_or(Value::Null)).context("invalid sequence")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_60(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "visualRevision"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_visual_render_receipt(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("visualRevision").cloned().unwrap_or(Value::Null)).context("invalid visualRevision")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_61(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_create(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_62(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_refresh(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_63(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "afterSeq", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_events_after(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("afterSeq").cloned().unwrap_or(Value::Null)).context("invalid afterSeq")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_64(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "afterSequence", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_artifacts_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("afterSequence").cloned().unwrap_or(Value::Null)).context("invalid afterSequence")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_65(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "artifactId", "offset", "length"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_artifact_read_range(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("artifactId").cloned().unwrap_or(Value::Null)).context("invalid artifactId")?, serde_json::from_value(args.get("offset").cloned().unwrap_or(Value::Null)).context("invalid offset")?, serde_json::from_value(args.get("length").cloned().unwrap_or(Value::Null)).context("invalid length")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_66(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "afterFrameSequence"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_frames_latest(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("afterFrameSequence").cloned().unwrap_or(Value::Null)).context("invalid afterFrameSequence")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_67(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "seed", "beforeFrameSequence", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_frames_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("seed").cloned().unwrap_or(Value::Null)).context("invalid seed")?, serde_json::from_value(args.get("beforeFrameSequence").cloned().unwrap_or(Value::Null)).context("invalid beforeFrameSequence")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_68(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "seed", "frameSequence"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_frame_content(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("seed").cloned().unwrap_or(Value::Null)).context("invalid seed")?, serde_json::from_value(args.get("frameSequence").cloned().unwrap_or(Value::Null)).context("invalid frameSequence")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_69(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "sliceId", "atSeq"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_get_state(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("sliceId").cloned().unwrap_or(Value::Null)).context("invalid sliceId")?, serde_json::from_value(args.get("atSeq").cloned().unwrap_or(Value::Null)).context("invalid atSeq")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_70(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "slices", "atSeq"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_get_state_batch(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("slices").cloned().unwrap_or(Value::Null)).context("invalid slices")?, serde_json::from_value(args.get("atSeq").cloned().unwrap_or(Value::Null)).context("invalid atSeq")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_71(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_relationships(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_72(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_cancel(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_73(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_pause(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_74(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_resume(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_75(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_open_visual(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_76(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_import_local(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_77(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_reconcile_cloud(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_78(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["algorithm", "status", "limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_list_cloud(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("algorithm").cloned().unwrap_or(Value::Null)).context("invalid algorithm")?, serde_json::from_value(args.get("status").cloned().unwrap_or(Value::Null)).context("invalid status")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_79(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_saved_loras_search(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_80(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_checkpoints_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_81(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_run_outputs(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_82(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_training_models(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_83(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["checkpointId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_saved_lora_archive(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("checkpointId").cloned().unwrap_or(Value::Null)).context("invalid checkpointId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_84(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["checkpointId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_saved_lora_download(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("checkpointId").cloned().unwrap_or(Value::Null)).context("invalid checkpointId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_85(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_saved_lora_import(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_86(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_checkpoint_infer(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_87(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["checkpointId", "patch"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_saved_lora_patch(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("checkpointId").cloned().unwrap_or(Value::Null)).context("invalid checkpointId")?, serde_json::from_value(args.get("patch").cloned().unwrap_or(Value::Null)).context("invalid patch")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_88(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["checkpointId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_saved_lora_publish(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("checkpointId").cloned().unwrap_or(Value::Null)).context("invalid checkpointId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_89(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_training_reconcile(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_90(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["optimizerRunId", "action", "checkpointId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers_container_experiment_action(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optimizerRunId").cloned().unwrap_or(Value::Null)).context("invalid optimizerRunId")?, serde_json::from_value(args.get("action").cloned().unwrap_or(Value::Null)).context("invalid action")?, serde_json::from_value(args.get("checkpointId").cloned().unwrap_or(Value::Null)).context("invalid checkpointId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_91(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["pluginId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::plugins_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("pluginId").cloned().unwrap_or(Value::Null)).context("invalid pluginId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_92(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::plugins_list(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_93(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["operation", "pluginId", "version", "sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::plugins_manage(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("operation").cloned().unwrap_or(Value::Null)).context("invalid operation")?, serde_json::from_value(args.get("pluginId").cloned().unwrap_or(Value::Null)).context("invalid pluginId")?, serde_json::from_value(args.get("version").cloned().unwrap_or(Value::Null)).context("invalid version")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_94(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["pluginId", "channel"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::plugins_set_release_channel(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("pluginId").cloned().unwrap_or(Value::Null)).context("invalid pluginId")?, serde_json::from_value(args.get("channel").cloned().unwrap_or(Value::Null)).context("invalid channel")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_95(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["settings"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::jesterky_analysis_settings(serde_json::from_value(args.get("settings").cloned().unwrap_or(Value::Null)).context("invalid settings")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_96(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::computer_use_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_97(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::computer_use_install(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_98(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::computer_use_remove(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_99(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["bundleId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::computer_use_revoke_app(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("bundleId").cloned().unwrap_or(Value::Null)).context("invalid bundleId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_100(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["permissionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::computer_use_open_settings(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("permissionId").cloned().unwrap_or(Value::Null)).context("invalid permissionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_101(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::browser_runtime_status().await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_102(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["origin"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::browser_policy_allow_origin(serde_json::from_value(args.get("origin").cloned().unwrap_or(Value::Null)).context("invalid origin")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_103(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["origin"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::browser_policy_revoke_origin(serde_json::from_value(args.get("origin").cloned().unwrap_or(Value::Null)).context("invalid origin")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_104(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visual_subscription_ready(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_105(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visual_stream_poll(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_106(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visual_media_read(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_107(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::diagnostics_report(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_108(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::diagnostics_status(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_109(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::diagnostics_query(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_110(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::diagnostics_explain(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_111(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::diagnostics_bundle(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_112(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::diagnostics_clear_index(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_113(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/manager.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::manager::optimizer_sidecar_status(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_114(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/manager.rs
            let allowed: &[&str] = &["version"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::manager::optimizer_sidecar_install(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("version").cloned().unwrap_or(Value::Null)).context("invalid version")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_115(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/manager.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::manager::optimizer_sidecar_start(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_116(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/manager.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::manager::optimizer_sidecar_stop(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_117(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/manager.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::manager::optimizer_sidecar_version(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_118(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/manager.rs
            let allowed: &[&str] = &["version"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::manager::optimizer_sidecar_uninstall(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("version").cloned().unwrap_or(Value::Null)).context("invalid version")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_119(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["genre"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_templates_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("genre").cloned().unwrap_or(Value::Null)).context("invalid genre")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_120(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["templateId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_templates_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("templateId").cloned().unwrap_or(Value::Null)).context("invalid templateId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_121(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_122(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_123(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_engine(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_124(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_presentation_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_125(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "presentation"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_presentation_put(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("presentation").cloned().unwrap_or(Value::Null)).context("invalid presentation")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_126(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_snapshots_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_127(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "snapshot"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_snapshot_put(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("snapshot").cloned().unwrap_or(Value::Null)).context("invalid snapshot")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_128(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_recordings_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_129(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "recording"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_recording_put(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("recording").cloned().unwrap_or(Value::Null)).context("invalid recording")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_130(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["observation"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_observation_report(serde_json::from_value(args.get("observation").cloned().unwrap_or(Value::Null)).context("invalid observation")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_131(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_revisions(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_132(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_annotations_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_133(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_annotation_create(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_134(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_seals_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_135(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "revision"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_seal(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("revision").cloned().unwrap_or(Value::Null)).context("invalid revision")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_136(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["receiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_seal_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("receiptDigest").cloned().unwrap_or(Value::Null)).context("invalid receiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_137(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["receiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_upload_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("receiptDigest").cloned().unwrap_or(Value::Null)).context("invalid receiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_138(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["receiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_share_seal(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("receiptDigest").cloned().unwrap_or(Value::Null)).context("invalid receiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_139(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["committedUrl"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_open_shared(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("committedUrl").cloned().unwrap_or(Value::Null)).context("invalid committedUrl")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_140(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_create(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_141(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_update(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_142(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "tsx"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_save(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("tsx").cloned().unwrap_or(Value::Null)).context("invalid tsx")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_143(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "title", "sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_fork(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("title").cloned().unwrap_or(Value::Null)).context("invalid title")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_144(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_archive(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_145(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_show(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_146(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_content(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_147(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_renditions(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_148(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId", "format", "theme", "sizeClass"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_rendition(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?, serde_json::from_value(args.get("format").cloned().unwrap_or(Value::Null)).context("invalid format")?, serde_json::from_value(args.get("theme").cloned().unwrap_or(Value::Null)).context("invalid theme")?, serde_json::from_value(args.get("sizeClass").cloned().unwrap_or(Value::Null)).context("invalid sizeClass")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_149(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["visualId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals_render(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("visualId").cloned().unwrap_or(Value::Null)).context("invalid visualId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_150(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_151(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_preview(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_152(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_session_open(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_153(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_show(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_154(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_answer_set(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_155(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["sessionId", "expectedRevision", "questionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_answer_clear(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("expectedRevision").cloned().unwrap_or(Value::Null)).context("invalid expectedRevision")?, serde_json::from_value(args.get("questionId").cloned().unwrap_or(Value::Null)).context("invalid questionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_156(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_comment_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_157(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_audio_begin(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_158(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_audio_append(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_159(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_audio_finish(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_160(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["sessionId", "attachmentId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_audio_read(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("attachmentId").cloned().unwrap_or(Value::Null)).context("invalid attachmentId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_161(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_audio_transcribe(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_162(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_transcript_correct(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_163(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_submit(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_164(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_165(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["id"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_166(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_cancel(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_167(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_export(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_168(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_campaign_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_169(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["campaignId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_campaign_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("campaignId").cloned().unwrap_or(Value::Null)).context("invalid campaignId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_170(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_campaign_close(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_171(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_campaign_adjudicate(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_172(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/human_annotations/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::human_annotations::human_annotation_supersede(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_173(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_174(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_175(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "revision"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_revision_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("revision").cloned().unwrap_or(Value::Null)).context("invalid revision")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_176(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "revision"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_validate(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("revision").cloned().unwrap_or(Value::Null)).context("invalid revision")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_177(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_pin_all(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_178(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_create(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_179(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_update(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_180(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_archive(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_181(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_restore(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_182(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_visibility_requests(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_183(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_visibility_request(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_184(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["requestId", "approved"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_visibility_decide(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("requestId").cloned().unwrap_or(Value::Null)).context("invalid requestId")?, serde_json::from_value(args.get("approved").cloned().unwrap_or(Value::Null)).context("invalid approved")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_185(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "revision"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_seal(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("revision").cloned().unwrap_or(Value::Null)).context("invalid revision")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_186(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_seals_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_187(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["receiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_seal_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("receiptDigest").cloned().unwrap_or(Value::Null)).context("invalid receiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_188(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["leftDigest", "rightDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_seals_compare(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("leftDigest").cloned().unwrap_or(Value::Null)).context("invalid leftDigest")?, serde_json::from_value(args.get("rightDigest").cloned().unwrap_or(Value::Null)).context("invalid rightDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_189(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_experiments_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_190(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_experiment_upsert(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_191(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["query"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_192(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["experimentId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("experimentId").cloned().unwrap_or(Value::Null)).context("invalid experimentId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_193(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_attach_evidence(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_194(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_195(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_create_child(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_196(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_relate(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_197(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "experimentId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_activate(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("experimentId").cloned().unwrap_or(Value::Null)).context("invalid experimentId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_198(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_update(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_199(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::experiments_finalize(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_200(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["query", "experimentId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::research_log_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("query").cloned().unwrap_or(Value::Null)).context("invalid query")?, serde_json::from_value(args.get("experimentId").cloned().unwrap_or(Value::Null)).context("invalid experimentId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_201(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::research_log_append(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_202(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_log_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_203(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_log_append(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_204(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["receiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_upload_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("receiptDigest").cloned().unwrap_or(Value::Null)).context("invalid receiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_205(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["ReceiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_share(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("ReceiptDigest").cloned().unwrap_or(Value::Null)).context("invalid ReceiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_206(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["publicationId", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_audience_set(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("publicationId").cloned().unwrap_or(Value::Null)).context("invalid publicationId")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_207(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["publicationId", "receiptDigest"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_audience_revoke(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("publicationId").cloned().unwrap_or(Value::Null)).context("invalid publicationId")?, serde_json::from_value(args.get("receiptDigest").cloned().unwrap_or(Value::Null)).context("invalid receiptDigest")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_208(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["PublicationId", "Slug"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_promote(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("PublicationId").cloned().unwrap_or(Value::Null)).context("invalid PublicationId")?, serde_json::from_value(args.get("Slug").cloned().unwrap_or(Value::Null)).context("invalid Slug")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_209(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["committedUrl"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_open_shared(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("committedUrl").cloned().unwrap_or(Value::Null)).context("invalid committedUrl")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_210(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "revision"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_comments_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("revision").cloned().unwrap_or(Value::Null)).context("invalid revision")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_211(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["reportId", "revision", "request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::reports_comment_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("reportId").cloned().unwrap_or(Value::Null)).context("invalid reportId")?, serde_json::from_value(args.get("revision").cloned().unwrap_or(Value::Null)).context("invalid revision")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_212(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::synth_config_get(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_213(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_catalog_get().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_214(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_catalog_refresh().await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_215(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::synth_config_update(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_216(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["windowMinutes"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_performance_get(serde_json::from_value(args.get("windowMinutes").cloned().unwrap_or(Value::Null)).context("invalid windowMinutes")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_217(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_begin_sign_in(app.clone(), app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_218(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_get_summary(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_219(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_refresh(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_220(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["action", "tier"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_open_billing(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("action").cloned().unwrap_or(Value::Null)).context("invalid action")?, serde_json::from_value(args.get("tier").cloned().unwrap_or(Value::Null)).context("invalid tier")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_221(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_poll_sign_in(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_222(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_cancel_sign_in(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_223(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::account_sign_out(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_224(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_oauth_begin(app.clone(), app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_225(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["redirectUrl"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_oauth_complete_manual(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("redirectUrl").cloned().unwrap_or(Value::Null)).context("invalid redirectUrl")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_226(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_oauth_ensure_ready(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_227(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_oauth_status(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_228(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_oauth_disconnect(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_229(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_oauth_cancel(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_230(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_multi_agent_list().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_231(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::model_multi_agent_update(serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_232(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_access_get().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_233(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_access_update(serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_234(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::desktop_permissions_get().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_235(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::desktop_permissions_update(serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_236(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_237(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "proposedAccess"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_choose_and_attach(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("proposedAccess").cloned().unwrap_or(Value::Null)).context("invalid proposedAccess")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_238(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_recent_folders(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_239(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_attach_recent(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_240(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_remove_attachment(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_241(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "path", "access", "reason"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_request_agent_grant(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?, serde_json::from_value(args.get("access").cloned().unwrap_or(Value::Null)).context("invalid access")?, serde_json::from_value(args.get("reason").cloned().unwrap_or(Value::Null)).context("invalid reason")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_242(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_grants_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_243(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["requestId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_approve_request(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("requestId").cloned().unwrap_or(Value::Null)).context("invalid requestId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_244(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["requestId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_scope_deny_request(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("requestId").cloned().unwrap_or(Value::Null)).context("invalid requestId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_245(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/migration/commands.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::storage::legacy_migration::commands::migration_scan(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_246(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/migration/commands.rs
            let allowed: &[&str] = &["sourcePath"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::storage::legacy_migration::commands::migration_prepare(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sourcePath").cloned().unwrap_or(Value::Null)).context("invalid sourcePath")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_247(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/migration/commands.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::storage::legacy_migration::commands::migration_apply(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_248(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/migration/commands.rs
            let allowed: &[&str] = &["confirmationToken"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::storage::legacy_migration::commands::migration_cancel(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("confirmationToken").cloned().unwrap_or(Value::Null)).context("invalid confirmationToken")?);
            Ok(json!({"result": result}))
    })
}

fn operation_249(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_get_status(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_250(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_reload(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_251(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["checkpointId", "modelId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_register_policy(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("checkpointId").cloned().unwrap_or(Value::Null)).context("invalid checkpointId")?, serde_json::from_value(args.get("modelId").cloned().unwrap_or(Value::Null)).context("invalid modelId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_252(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_policies(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_253(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_adapter_status().await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_254(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["modelId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_adapter_download(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("modelId").cloned().unwrap_or(Value::Null)).context("invalid modelId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_255(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_models_list(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_256(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_models_set_directory(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_257(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna_models_clear_directory(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_258(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_inference_snapshot(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_259(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_inference_stream_start(app.clone(), app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_260(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_inference_stream_stop(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_261(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_model_unload(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_262(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &["modelId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_model_download(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("modelId").cloned().unwrap_or(Value::Null)).context("invalid modelId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_263(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &["modelId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_model_delete(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("modelId").cloned().unwrap_or(Value::Null)).context("invalid modelId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_264(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_settings_snapshot(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_265(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/laguna.rs
            let allowed: &[&str] = &["patch"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::laguna::laguna_settings_update(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("patch").cloned().unwrap_or(Value::Null)).context("invalid patch")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_266(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_models.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_models::training_models_list();
            Ok(json!({"result": result}))
    })
}

fn operation_267(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_models.rs
            let allowed: &[&str] = &["modelId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_models::training_models_download(app.clone(), serde_json::from_value(args.get("modelId").cloned().unwrap_or(Value::Null)).context("invalid modelId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_268(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_models.rs
            let allowed: &[&str] = &["modelId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_models::training_models_delete(serde_json::from_value(args.get("modelId").cloned().unwrap_or(Value::Null)).context("invalid modelId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_269(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/mlx_runtime.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::mlx_runtime::training_mlx_runtime_status();
            Ok(json!({"result": result}))
    })
}

fn operation_270(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/optimizers/mlx_runtime.rs
            let allowed: &[&str] = &["confirm"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::optimizers::mlx_runtime::training_mlx_runtime_install(app.clone(), serde_json::from_value(args.get("confirm").cloned().unwrap_or(Value::Null)).context("invalid confirm")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_271(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_artifacts.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_artifacts::training_artifacts_list();
            Ok(json!({"result": result}))
    })
}

fn operation_272(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_artifacts.rs
            let allowed: &[&str] = &["id"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_artifacts::training_artifacts_get(serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_273(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_artifacts.rs
            let allowed: &[&str] = &["id", "destination", "expectedDigest", "confirm"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_artifacts::training_artifacts_export(serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?, serde_json::from_value(args.get("destination").cloned().unwrap_or(Value::Null)).context("invalid destination")?, serde_json::from_value(args.get("expectedDigest").cloned().unwrap_or(Value::Null)).context("invalid expectedDigest")?, serde_json::from_value(args.get("confirm").cloned().unwrap_or(Value::Null)).context("invalid confirm")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_274(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/training_artifacts.rs
            let allowed: &[&str] = &["id", "confirm"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_artifacts::training_artifacts_delete(serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?, serde_json::from_value(args.get("confirm").cloned().unwrap_or(Value::Null)).context("invalid confirm")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_275(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["id", "message", "confirm"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::training_artifacts_launch_inference(serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?, serde_json::from_value(args.get("message").cloned().unwrap_or(Value::Null)).context("invalid message")?, serde_json::from_value(args.get("confirm").cloned().unwrap_or(Value::Null)).context("invalid confirm")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_276(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_models_list();
            Ok(json!({"result": result}))
    })
}

fn operation_277(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &["id"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_model_download(app.clone(), serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_278(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &["id"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_models_set_selected(serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_279(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &["id"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_models_clear(serde_json::from_value(args.get("id").cloned().unwrap_or(Value::Null)).context("invalid id")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_280(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_runtime_status(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_281(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_runtime_warm(app.clone(), app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_282(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &["audioPath"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_transcribe(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("audioPath").cloned().unwrap_or(Value::Null)).context("invalid audioPath")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_283(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/whisper.rs
            let allowed: &[&str] = &["audioBase64", "mimeType"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::whisper::whisper_transcribe_base64(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("audioBase64").cloned().unwrap_or(Value::Null)).context("invalid audioBase64")?, serde_json::from_value(args.get("mimeType").cloned().unwrap_or(Value::Null)).context("invalid mimeType")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_284(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/skills.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::skills::skills_list();
            Ok(json!({"result": result}))
    })
}

fn operation_285(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_snapshot(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_286(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace", "content"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_workspace_agents_update(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?, serde_json::from_value(args.get("content").cloned().unwrap_or(Value::Null)).context("invalid content")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_287(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace", "skillId", "enabled", "content"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_skill_update(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?, serde_json::from_value(args.get("skillId").cloned().unwrap_or(Value::Null)).context("invalid skillId")?, serde_json::from_value(args.get("enabled").cloned().unwrap_or(Value::Null)).context("invalid enabled")?, serde_json::from_value(args.get("content").cloned().unwrap_or(Value::Null)).context("invalid content")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_288(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace", "groupId", "enabled"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_mcp_group_update(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?, serde_json::from_value(args.get("groupId").cloned().unwrap_or(Value::Null)).context("invalid groupId")?, serde_json::from_value(args.get("enabled").cloned().unwrap_or(Value::Null)).context("invalid enabled")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_289(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_cookbooks_install(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_290(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_cookbooks_cancel(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_291(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace", "enabled"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_cookbooks_set_enabled(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?, serde_json::from_value(args.get("enabled").cloned().unwrap_or(Value::Null)).context("invalid enabled")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_292(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/context.rs
            let allowed: &[&str] = &["workspace"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::context::context_cookbooks_uninstall(serde_json::from_value(args.get("workspace").cloned().unwrap_or(Value::Null)).context("invalid workspace")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_293(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::workspace_choose_directory(app.clone()).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_294(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_session_start(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_295(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_turn_start(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_296(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["mut request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_turn_send(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("mut request").cloned().unwrap_or(Value::Null)).context("invalid mut request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_297(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["sessionId", "model"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::synth_cloud_inference_status(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("model").cloned().unwrap_or(Value::Null)).context("invalid model")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_298(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_turn_interrupt(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_299(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_thread_compact(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_300(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_thread_read(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_301(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_thread_items_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_302(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_turn_steer(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_303(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_approval_resolve(app.clone(), app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_304(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_session_close(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_305(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_sessions_list(app.clone(), app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_306(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::codex_default_workspace().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_307(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_create(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_308(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["workspaceId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("workspaceId").cloned().unwrap_or(Value::Null)).context("invalid workspaceId")?);
            Ok(json!({"result": result}))
    })
}

fn operation_309(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId", "afterSequence"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_snapshot(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?, serde_json::from_value(args.get("afterSequence").cloned().unwrap_or(Value::Null)).context("invalid afterSequence")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_310(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId", "data"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_write(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?, serde_json::from_value(args.get("data").cloned().unwrap_or(Value::Null)).context("invalid data")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_311(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId", "cols", "rows"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_resize(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?, serde_json::from_value(args.get("cols").cloned().unwrap_or(Value::Null)).context("invalid cols")?, serde_json::from_value(args.get("rows").cloned().unwrap_or(Value::Null)).context("invalid rows")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_312(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_ghostty_mount(app.get_webview_window("main").context("desktop_required: attach the desktop first")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_313(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId", "frame"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_ghostty_set_frame(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?, serde_json::from_value(args.get("frame").cloned().unwrap_or(Value::Null)).context("invalid frame")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_314(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId", "visible"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_ghostty_set_visible(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?, serde_json::from_value(args.get("visible").cloned().unwrap_or(Value::Null)).context("invalid visible")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_315(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_ghostty_focus(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_316(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_ghostty_unmount(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_317(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/lib.rs
            let allowed: &[&str] = &["terminalId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::terminal_close(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("terminalId").cloned().unwrap_or(Value::Null)).context("invalid terminalId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_318(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_workspace_roots_list(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_319(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_bindings_list(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_320(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_locators_list(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_321(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["pickerPath", "provider", "variable", "label"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_locator_remember_external(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("pickerPath").cloned().unwrap_or(Value::Null)).context("invalid pickerPath")?, serde_json::from_value(args.get("provider").cloned().unwrap_or(Value::Null)).context("invalid provider")?, serde_json::from_value(args.get("variable").cloned().unwrap_or(Value::Null)).context("invalid variable")?, serde_json::from_value(args.get("label").cloned().unwrap_or(Value::Null)).context("invalid label")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_322(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["locatorId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_locator_register(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("locatorId").cloned().unwrap_or(Value::Null)).context("invalid locatorId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_323(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["locatorId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_locator_forget(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("locatorId").cloned().unwrap_or(Value::Null)).context("invalid locatorId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_324(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["provider", "scope"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("provider").cloned().unwrap_or(Value::Null)).context("invalid provider")?, serde_json::from_value(args.get("scope").cloned().unwrap_or(Value::Null)).context("invalid scope")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_325(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_create(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_326(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["secretId", "value"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_replace(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("secretId").cloned().unwrap_or(Value::Null)).context("invalid secretId")?, serde_json::from_value(args.get("value").cloned().unwrap_or(Value::Null)).context("invalid value")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_327(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["secretId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_delete(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("secretId").cloned().unwrap_or(Value::Null)).context("invalid secretId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_328(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["secretId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_test(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("secretId").cloned().unwrap_or(Value::Null)).context("invalid secretId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_329(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_request_use(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_330(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["secretId", "runId", "recipeId", "rememberRecipe", "requestedPolicy", "requestId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_grant_use(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("secretId").cloned().unwrap_or(Value::Null)).context("invalid secretId")?, serde_json::from_value(args.get("runId").cloned().unwrap_or(Value::Null)).context("invalid runId")?, serde_json::from_value(args.get("recipeId").cloned().unwrap_or(Value::Null)).context("invalid recipeId")?, serde_json::from_value(args.get("rememberRecipe").cloned().unwrap_or(Value::Null)).context("invalid rememberRecipe")?, serde_json::from_value(args.get("requestedPolicy").cloned().unwrap_or(Value::Null)).context("invalid requestedPolicy")?, serde_json::from_value(args.get("requestId").cloned().unwrap_or(Value::Null)).context("invalid requestId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_331(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["secretId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_deny_use(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("secretId").cloned().unwrap_or(Value::Null)).context("invalid secretId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_332(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_capabilities_list(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_333(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["capabilityId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_revoke_capability(app.try_state().context("runtime service is unavailable")?, app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("capabilityId").cloned().unwrap_or(Value::Null)).context("invalid capabilityId")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_334(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_request_env_import(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_335(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["requestId", "selected", "after", "confirm"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_commit_env_import(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("requestId").cloned().unwrap_or(Value::Null)).context("invalid requestId")?, serde_json::from_value(args.get("selected").cloned().unwrap_or(Value::Null)).context("invalid selected")?, serde_json::from_value(args.get("after").cloned().unwrap_or(Value::Null)).context("invalid after")?, serde_json::from_value(args.get("confirm").cloned().unwrap_or(Value::Null)).context("invalid confirm")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_336(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_audit_list(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_337(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_proxy_status(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_338(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_pending(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_339(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/secrets/mod.rs
            let allowed: &[&str] = &["requestId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::secrets::secrets_deny_env_import(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("requestId").cloned().unwrap_or(Value::Null)).context("invalid requestId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_340(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/telemetry/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::telemetry::product_telemetry_get_policy(app.try_state().context("runtime service is unavailable")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_341(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/telemetry/mod.rs
            let allowed: &[&str] = &["optOut"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::telemetry::product_telemetry_set_opt_out(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("optOut").cloned().unwrap_or(Value::Null)).context("invalid optOut")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_342(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/tauri.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::tauri::failures_query(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_343(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/tauri.rs
            let allowed: &[&str] = &["failureId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::tauri::failures_get(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("failureId").cloned().unwrap_or(Value::Null)).context("invalid failureId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_344(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/tauri.rs
            let allowed: &[&str] = &["failureId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::tauri::failures_timeline(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("failureId").cloned().unwrap_or(Value::Null)).context("invalid failureId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_345(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/tauri.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::tauri::logs_query(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_346(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/tauri.rs
            let allowed: &[&str] = &["failureId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::tauri::failure_export_bundle(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("failureId").cloned().unwrap_or(Value::Null)).context("invalid failureId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_347(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/adapters/tauri.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::adapters::tauri::observability_status(app.try_state().context("runtime service is unavailable")?);
            Ok(json!({"result": result}))
    })
}

fn operation_348(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/telemetry/mod.rs
            let allowed: &[&str] = &["granted"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::telemetry::product_telemetry_set_consent(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("granted").cloned().unwrap_or(Value::Null)).context("invalid granted")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_349(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/telemetry/mod.rs
            let allowed: &[&str] = &["limit"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::telemetry::product_telemetry_recent(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("limit").cloned().unwrap_or(Value::Null)).context("invalid limit")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_350(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/telemetry/mod.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::telemetry::product_telemetry_flush_now(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_351(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/documents/commands.rs
            let allowed: &[&str] = &["sessionId", "path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::documents::commands::workspace_read_file(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_352(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/documents/commands.rs
            let allowed: &[&str] = &["sessionId", "path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::documents::commands::workspace_list_dir(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_353(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/documents/commands.rs
            let allowed: &[&str] = &["sessionId", "path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::documents::commands::document_show(app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_354(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/visuals/user_templates.rs
            let allowed: &[&str] = &["templateId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals::user_templates::visuals_template_shell_source(serde_json::from_value(args.get("templateId").cloned().unwrap_or(Value::Null)).context("invalid templateId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_355(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/visuals/user_templates.rs
            let allowed: &[&str] = &["sessionId", "templateId", "manifest", "source"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals::user_templates::visuals_template_save(app.clone(), serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("templateId").cloned().unwrap_or(Value::Null)).context("invalid templateId")?, serde_json::from_value(args.get("manifest").cloned().unwrap_or(Value::Null)).context("invalid manifest")?, serde_json::from_value(args.get("source").cloned().unwrap_or(Value::Null)).context("invalid source")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_356(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/visuals/user_templates.rs
            let allowed: &[&str] = &["sessionId", "templateId", "fromTemplateId", "title"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals::user_templates::visuals_template_create(app.clone(), serde_json::from_value(args.get("sessionId").cloned().unwrap_or(Value::Null)).context("invalid sessionId")?, serde_json::from_value(args.get("templateId").cloned().unwrap_or(Value::Null)).context("invalid templateId")?, serde_json::from_value(args.get("fromTemplateId").cloned().unwrap_or(Value::Null)).context("invalid fromTemplateId")?, serde_json::from_value(args.get("title").cloned().unwrap_or(Value::Null)).context("invalid title")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_357(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/visuals/user_templates.rs
            let allowed: &[&str] = &["templateId"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::visuals::user_templates::visuals_template_validate(serde_json::from_value(args.get("templateId").cloned().unwrap_or(Value::Null)).context("invalid templateId")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_358(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/approval_inspection.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::approval::inspection::approvals_pending(app.try_state().context("runtime service is unavailable")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_359(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/session/approval_inspection.rs
            let allowed: &[&str] = &["request"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::session::approval::inspection::approvals_approve_digest(app.clone(), app.try_state().context("runtime service is unavailable")?, serde_json::from_value(args.get("request").cloned().unwrap_or(Value::Null)).context("invalid request")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_360(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/project_sources/commands.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::project_sources::commands::project_sources_get().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_361(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/project_sources/commands.rs
            let allowed: &[&str] = &[];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::project_sources::commands::project_sources_refresh().map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_362(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/project_sources/commands.rs
            let allowed: &[&str] = &["containers", "recipes"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::project_sources::commands::project_source_add(app.clone(), serde_json::from_value(args.get("containers").cloned().unwrap_or(Value::Null)).context("invalid containers")?, serde_json::from_value(args.get("recipes").cloned().unwrap_or(Value::Null)).context("invalid recipes")?).await.map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}

fn operation_363(app: &tauri::AppHandle, args: Value) -> Reply<'_> {
    Box::pin(async move {
            anyhow::ensure!(args.is_object(), "operation arguments must be an object");
            // Handler: apps/synth_desktop/src-tauri/src/project_sources/commands.rs
            let allowed: &[&str] = &["path"];
            anyhow::ensure!(args.as_object().unwrap().keys().all(|key| allowed.contains(&key.as_str())), "unknown operation argument");
            let result = crate::project_sources::commands::project_source_remove(serde_json::from_value(args.get("path").cloned().unwrap_or(Value::Null)).context("invalid path")?).map_err(|error| anyhow::anyhow!(format!("{error:?}")))?;
            Ok(json!({"result": result}))
    })
}
