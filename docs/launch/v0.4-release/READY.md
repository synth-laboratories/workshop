# v0.4 readiness

Status: **released and production-verified**.

The source consolidation, Banking77 producer contract, frozen-head automated suites, exact friends artifact, installed-artifact CUA, paid Banking77 GEPA acceptance, optimizer visual, full advanced-trace review, provider matrix, sidecar recovery, forced app restart, and production download verification are complete.

Workshop was promoted through `dev` to `main`, tagged `v0.4.0`, and published as a GitHub release. The website was promoted through `dev` to `main`, `SYNTH_DESKTOP_STABLE_VERSION=0.4.0` was activated, and the public route and checksum were verified. The v0.5 implementation branches now start from the reconciled released state.
