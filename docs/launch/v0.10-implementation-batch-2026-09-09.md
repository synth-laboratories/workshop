# v0.10 implementation batch — 2026-09-09

Worktree: `wt-v010-production-parity`, branch `codex/v010-production-parity`,
based on planning commit `010be6a1`. The certified original worktree is preserved.
This is an implementation receipt, **not a release GO**.

## Implemented

- Ported production telemetry pipeline from `e63d2b29`, preserving v0.10 paid
  compute settings, account storage notifications, composer ownership, and
  observability commands. Regenerated 351 native command bindings, dispatch,
  and tool schemas from their canonical generators.
- Added durable outbox sequence allocation across deletion, consent withdrawal,
  and acknowledgment races. Serialized manual/background flushes. Revalidated
  legacy payloads before egress and filtered eligibility before applying limits.
- Privacy controls reflect actual sync consent, retain a failed consent question
  for retry, distinguish event-read failure from an empty outbox, and disclose
  possible signed-in account association without promising anonymity.
- Helper notarization supports an App Store Connect key file without Keychain.
  A configured Keychain profile is refused absent an explicit-operation opt-in.
  No credential was read and no signing/notarization submission was performed.
- Backend branch `codex/v010-release-hardening`, commit `19e46ac56`, corrects
  deployment guidance to name Railway pre-deploy migrations and schema rollback
  constraints. It does not change deployment or migration execution.
- Separate `wt-containers-v010-stable` and `wt-optimizers-v010-stable` worktrees
  prepare stable package versions. TBLite has an independent evaluation lock;
  its unavailable research fixtures are explicitly outside production tests.
  Workshop's embedded Optimizers source/version remain unchanged.

## Verified at this batch

- Native telemetry: 26 passed, including deletion before acknowledgment,
  re-consent, invalid legacy rows, retry, essential locality, and privacy gates.
- Browser privacy: five passed, including failed-save retry and read-error UI.
- Renderer typecheck and production build passed.
- Existing accessibility/source checks: 722 passed before adding the four
  notarization selection tests; all four new tests independently passed.
- Canonical dispatch and schema drift checks passed (351 commands).
- Containers affected contract suite: 20 passed; source Ruff passed.
- Both independent Optimizers uv locks resolve; production dependencies install
  stable Containers without TBLite. This is not yet a public-index install.

## Still open

- Full integrated native/package suite results and artifact checks; tests and
  builds are running. Initial Containers full run reproduced eight stale
  contract expectations (now corrected) plus one timing-floor failure under
  competing builds. No timing thresholds were relaxed.
- Full parity audit against shipped v0.9.3 source `fd9b5bb3…` and public main,
  release tooling reconciliation, branch ancestry, final packaged native/CUA
  certification, migration rehearsal and exact service/deployment inventory.
- GitHub PR/release API access previously returned 403; non-Keychain release
  and Railway/Apple credential paths remain unverified. Local absence of a
  token does not prove protected CI secrets are absent.
- Public package publishing, branch merges, staging/production deployments,
  Developer-ID signing/notarization, desktop upload, catalog promotion, and
  public installation remain undone. No public tag or deployment was created.

TBLite publication, paid experiments, Reports hosting, Jesterky distribution,
and the embedded Optimizers repin remain outside this release batch's scope.
