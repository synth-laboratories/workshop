# v0.2 E2E suite

**Updated:** 2026-08-13
**Purpose:** Fast GO gates. Fixture replay and four live checks. Not paid A1 10×, Harbor A2 rerun, or public dig.bench.

Public stable stays `0.1.0` until GO. Canonical scope: [`v0.2-launch.md`](./v0.2-launch.md) **v0.2 GO scope**.

---

## What this suite proves

| Layer | What it covers | Command |
| --- | --- | --- |
| Node — family truth | Slot `stream` only; control records are not evidence; missing reward/usage stay missing; guessed `/events` refused; dig.bench has no frames; two run_ids do not mix | `node --experimental-strip-types --test visuals/tests/v02_family_truth.test.mjs` |
| Node — MCP + surface | Visuals MCP advertises only **implemented** tools (bin is source of truth: compact `visual_manage`, no `resources/*`, no unimplemented `visual_stream_live_eval`). Today `v02_mcp_schema.test.mjs` still asserts stale `tools.json` — that is a G1 bug, not coverage. Approval cards pin above Working…; Full system access is `danger-full-access` | `node --test apps/synth_desktop/tests/v02_*.test.mjs` |
| Playwright `[v0.2]` | Craftax / Harbor / dig.bench fixture replay in the real shells; guessed stream URL fails closed; two live visuals stay isolated; pending approval UX; permission chip copy | `cd apps/synth_desktop && npx playwright test -g '\[v0.2\]'` |
| Bombadil | Approval card layout: Reject / Approve once above Working…, no horizontal overflow. Grouped W1 turn keeps named `synth_containers` / `synth_visuals` calls and the visual chip — never `used tools N calls`. | `BOMBADIL_SPEC=apps/synth_desktop/tests/bombadil/approval-card.spec.ts node apps/synth_desktop/tests/bombadil/run.mjs` and `…/grouped-visual-evidence.spec.ts` |

```bash
./scripts/v02-e2e-gates.sh
./scripts/desktop.sh conform
npm run desktop:check
```

Existing coverage this slice extends, not duplicates: `visual-responsive-gate.spec.ts`, `optimizer-banking77.spec.ts`, `visuals/tests/live_stream_contract.test.mjs`, `live_eval_reducer.test.mjs`, Bombadil `visual-pane-boundaries.spec.ts`.

---

## What this suite does not prove

- Paid A1 Craftax Luna 10×, Harbor A2 live, GEPA A3, SFT A4/A6, or public A8.
- Durable poll ≡ SSE ≡ WS (A5) against a live Containers façade.
- W1 ten-rollout golden path on a signed binary (already receipted on `v02golden`; do not rerun for GO).
- W3 poll 503 / frame 404 / policy-pin (already receipted; do not rerun).
- Tool-loop circuit breaker until SYN-3215 lands. The MCP schema `.mjs` currently locks a **stale** `tools.json` (advertises `visual_stream_live_eval`, forbids `visual_manage`) and must be retargeted at the bin before it counts.

Do not write a receipt that says A1/A2/A8 PASS from these fixtures.

---

## Live CUA — four checks (GO G3)

Isolated instance (`./scripts/desktop-instance.sh`). Not the prod profile. Independent tester. Record SHA in the receipt.

1. **Tool-loop** — Laguna S (or a model that emits `shell` / `resources/list` / nested `visual_manage`). Unsupported `shell` maps or fail-closed; MCP lists only implemented Visuals tools; malformed `visual_manage` corrective error; repeated identical failures stop; token/cost visible while looping. No paid Craftax.
2. **Approval** — Ask/untrusted; one shell command; card pins above Working… with Reject / Approve once; stop clears both.
3. **Visual honesty** — Craftax (Harbor if a sealed spool is local) from library or receipt. Missing is `—`, never `0`. `stream.subscribed` is not evidence. Two visuals do not mix. dig.bench fixture has no PNG frames (fixture only — not an A8 claim).
4. **Visual-first smoke** — find/register Craftax, bind visual, **one** rollout, visual updates. No guessed `/events`.

Permissions chip (Full system access ≠ Workspace access) stays in the Playwright `[v0.2]` slice; do not add a fifth live walk unless that spec is red.

W1 10×, W3 proxy drills, and live A8 are **not GO work**. Installed W1–W3 already PASS; do not rerun. Canonical receipt: [`w1-w3-tier4-installed.json`](../receipts/2026-08-13/w1-w3-tier4-installed.json). Finish path: [`V0.2_FINISH_HANDOFF_2026-08-13.md`](./V0.2_FINISH_HANDOFF_2026-08-13.md).

---

## Hard no-go (same as launch notes)

- Dirty tree used as the artifact source
- Guessed `/events` or slot `live` / `jobs` on an authoritative run
- Missing reward/usage coerced to 0
- Token in a receipt, screenshot, or Trace V5
- Public dig.bench claimed (template/mock ≠ A8)
- Independent reviewer is the implementer
- Repeated identical tool failures allowed to burn unbounded tokens
- Public stable flipped before provenance, hashes, and deliberate GO
