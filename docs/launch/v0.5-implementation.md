# v0.5 implementation

Status: **ready for implementation**.

## Branch point

The `v0.5/implementation` branch was created from the reconciled v0.4 release commit after the v0.4 branch reached `dev`, `main`, and tag `v0.4.0`.

| Repository | v0.5 branch point |
| --- | --- |
| Workshop | `e12276d05d4513ad9e74d93c2c54bbc616e92926` |
| Public frontend | `06943bd51d17531444ce44a083783da8fc94aa66` |
| Public cookbook | `9d82a6a8785a4bcf2a214ee8672f076063bb0f98` |

## Entry conditions

- [x] v0.4 installed-artifact and paid Banking77 CUA receipts are accepted.
- [x] Workshop, cookbook, frontend, Containers, optimizer, and testing pins are recorded in the v0.4 release receipts.
- [x] The production download route advertises v0.4 and serves the recorded SHA-256.
- [x] The v0.4 tag is an ancestor of `dev` and `main`.
- [x] Matching v0.5 implementation branches and clean local worktrees exist for Workshop, frontend, and cookbook.

## Initial v0.5 lanes

1. Replace the friends-only package path with Developer ID signing, notarization, stapling, and notarization receipts.
2. Move the stable release catalog and artifact publication into one atomic, checksum-verified promotion workflow.
3. Turn the Banking77 producer contract tests into shared cookbook conformance fixtures.
4. Eliminate the Rust/Clippy warning backlog and make warnings fatal for changed code.
5. Add an installed-artifact smoke harness that records version, bundle identity, diagnostics, trace navigation, and provider readiness without paid execution.
6. Make full Playwright surface-aware so hidden-surface controls cannot be mistaken for missing data.

## Working locations

- Workshop: `work/workshop-v05-implementation`
- Public frontend: `work/frontend-v05-implementation`
- Public cookbook: `work/cookbooks-v05-implementation`

All three worktrees track `origin/v0.5/implementation` and were clean at creation. New v0.5 work should enter through these branches; v0.4 remains immutable except for release-operational corrections.
