# Workshop v0.4 close

Updated 2026-08-16 from the final-cut readiness review.

## Frozen decisions

| Decision | v0.4 policy |
| --- | --- |
| Promotion | One reviewed `v0.4/*` branch per repository → `dev`; release PR `dev` → `main`; tag the released `main`; reconcile `main` back to `dev` before v0.5 branches. |
| Signing | Friends release: ad-hoc signed ZIP, explicitly unnotarized. Production/notarized claims are out of scope for this cut. |
| Public product name | `Synth Desktop`. `Synth Workshop v0.4 · <instance>` remains development-instance chrome only. |
| Workshop authority | `release/v0.4-final`, preserving the replay/capture history and adding TPS/elapsed and transcript-first Craftax trace inspection. |
| Cookbook authority | `v0.4/banking77-gepa-v2` from cookbook `dev`; unrelated dirty `main` work remains untouched. |

## Candidate composition

- Replay/capture contract line through `f8ccbc7`.
- TPS and elapsed-time repair ported from sibling `d2d03cc`.
- Transcript-first Craftax viewer ported from sibling `2ef275a` on top of the newer replay client.
- Stale hosted-SFT invariant updated to assert the current public sidecar and host-owned template contracts.
- User-visible v0.4 notes live in `v0p4_changelog.md` and Settings → About.

## Promotion sequence

1. Pin and review the Banking77 producer commit; no paid CUA before its focused contract tests pass.
2. Freeze Workshop after focused and full gates. Any blocker fix invalidates the affected gates.
3. Build only from the clean committed Workshop head and a clean normal Containers checkout at the pinned source revision.
4. Run installed-artifact Craftax replay, Banking77 GEPA, TPS/elapsed, trace-viewer, restart-replay, and compact/wide acceptance.
5. Merge each v0.4 branch into `dev`, promote `dev` to `main`, tag, publish the immutable artifact, and verify the public download/hash.
6. Reconcile `main` to `dev`, then create v0.5 implementation branches from that exact released state.

## Release blockers

- Full frozen-head Playwright, Rust, protocol, formatting, provider-smoke, fault-injection, and installed-artifact acceptance must be recorded in `docs/launch/v0.4-release/TEST_REPORT.md`.
- Banking77 paid CUA must not begin without explicit paid-compute approval and a fresh durable run directory.
- A public artifact URL and SHA-256 cannot be activated until the exact release bytes exist.
- Notarization is not performed by `scripts/release-artifact.sh`; do not describe this ZIP as notarized.
