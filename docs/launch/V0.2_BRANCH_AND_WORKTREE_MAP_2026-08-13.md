# v0.2 branch and worktree map — 2026-08-13

This note records the post-acceptance split between the v0.2 release lanes and
the preservation lanes created while closing the release. It supersedes the
branch tips in `V0.2_REMAINING_WORK_2026-08-13.md`.

## Release lanes

| Repository | Branch | Recorded tip | Purpose |
| --- | --- | --- | --- |
| Workshop | `josh/aug12-optimizers-workshop-visuals` | `e198edb` before this note | Desktop, visuals, Bombadil, deterministic gates, and release pipeline |
| Containers | `josh/aug12-containers-platform` | `668222c` | Platform, journal safety, detached Trace V5 capture, HealthBench, and Harbor |
| Optimizers | `josh/aug12-g1-gepa` | `df95901` | GEPA cost truth and the platform message-observer hook used by streamed optimizer deltas |
| Hosted optimizers | `agent/v02-hosted-release` | `0139406` | Clean `origin/main` integration of GELO/GoEx and SFT only; MAPO and OHCO history excluded |
| SFT source lane | `josh/aug12-sft` | `108954d` | Original SFT commits and fixture acceptance evidence |
| GameBench service lane | `feat/deo-bundle-pool-packaging` | `cc3088d` is the v0.2 service/source preservation commit; `b7bf0f9` is the sealed receipt fix | Craftax sealed Trace V5 receipts and Harbor service work |

The hosted release branch was assembled in the clean worktree
`Documents/Codex/2026-08-13/let/work/optimizers-beta-v02`. Its Rust acceptance
result was 51 GELO/GoEx tests plus 38 SFT tests, all passing. The SFT fixture
curation result was 55/55.

## Preserved, not part of the v0.2 merge

| Repository | Branch | Tip | Material preserved |
| --- | --- | --- | --- |
| optimizers-beta | `mapo-h2-preserve-20260706` | `933af0e` | MAPO, standalone hosted runners, tunnel work, Dockerfiles, and mixed GoEx experiments |
| GameBench fork remote `live` | `feat/deo-bundle-pool-packaging` | `c2e246e` | Separate commits for DungeonGrid/DEO, Nethack parity/oracles, Emerald parity, Sokoban Rust, SMB Gold, and remaining policy lanes |

The GameBench preservation commits after `cc3088d` are backups, not a request
to merge those projects into v0.2. They remain on the existing long-running
branch because transplanting its hundreds of unrelated commits onto GameBench
`main` would be unsafe.

## Deliberately local generated artifacts

These paths were not added to Git. They are generated evidence or raw oracle
material, total roughly 936 MiB, and are not release source:

- `optimizers-beta/examples/.out/`
- `gamebench/nle.ttyrec3.bz2`
- `gamebench/tasks/nethack-dlvl1-singleplayer/nle.ttyrec3.bz2`
- `gamebench/tasks/nethack-dlvl1-singleplayer/reports/`
- `gamebench/tasks/pokemon-emerald-littleroot-singleplayer/outputs/`

Do not use `git add -A` in either preservation worktree. Promote an individual
receipt only after reviewing its size, provenance, and credential content.

## Release rule

Only the four release branches in the first table should be merged for v0.2:
Workshop, Containers, Optimizers/GEPA, and Hosted optimizers. The SFT source and
GameBench service branches are source/evidence lanes whose required commits are
already represented by the hosted integration and deployed service work.
