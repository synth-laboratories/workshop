# Workshop v0.2 finish handoff — 2026-08-13

## Objective

Finish the v0.2 friends/public release from the current live-proven floor.
The remaining critical path is:

1. G1 agent/tool-loop fail-closed behavior and receipt.
2. G2 split and land the mixed repository trees on one clean release tip.
3. G3 run the deterministic gates and four live checks on that exact tip.
4. G4 produce the friends-class artifact, provenance/checksums, and deliberate GO.

Public dig.bench/A8 is explicitly deferred and is **not a v0.2 blocker**. Do
not obtain a token, run A8, or claim public dig.bench in release materials.

## Current authoritative state

Workshop:

- Path: `/Users/joshuapurtell/Documents/GitHub/workshop`
- Branch: `josh/aug12-optimizers-workshop-visuals`
- Handoff base: `84cc637c870bd22036566e5344eab2c0d916e0b3`
- No upstream is configured for this branch.
- `84cc637` closes installed-candidate W1–W3 and adds the canonical receipt.
- Public stable remains `0.1.0`; Desktop package metadata is already `0.2.0`.

Canonical launch scope is the **v0.2 GO scope** in
[`v0.2-launch.md`](./v0.2-launch.md). The canonical installed golden-path
receipt is
[`w1-w3-tier4-installed.json`](../receipts/2026-08-13/w1-w3-tier4-installed.json).

### Closed work — do not rerun by default

- A1/A5 Craftax execution and durable transport: PASS.
- A2 Harbor: PASS on the isolated Docker machine. Local OrbStack reproduction
  is parked; do not restart OrbStack while unrelated jobs exist.
- A3 dual GEPA: PASS.
- A4 hosted SFT occupancy: PASS.
- A6 fresh scored SFT campaign: PASS.
- A11/A12 reconnect and idempotency: PASS.
- O1–O4 operations and V5/V6 automated acceptance: PASS.
- W1 installed golden path: PASS, exactly 10/10 real Craftax rollouts.
- W2 installed visual revision: PASS, substantive revision 2 with 1280×900
  and 768×1024 reviews plus a later one-start smoke on the same installed SHA.
- W3: PASS. Missing-MCP safe-stop plus poll-503, immutable-frame-404, and
  policy-pin-403 all have receipts. The three proxy drills used exactly one
  engine start total.
- ChatGPT OAuth startup refresh fix: `859110f`; operator-confirmed Re-sync
  succeeded in the installed candidate. Never capture or copy its credential.

Only replay paid/live receipts if the final landed SHA materially changes the
relevant runtime. Do not re-spend A1/A3/A4/A6 merely because the trees are
being cleaned up.

## Remaining GO gates

### G1 — agent/tool-loop fail-closed (`SYN-3215`)

Pass all five behaviors and write a cheap live receipt using Laguna S or
another model that naturally emits the calls. No paid Craftax is needed.

1. A common unsupported `shell` attempt maps onto the supported execution tool
   or fails closed with a model-readable, non-retry-inviting error.
2. MCP advertises only implemented methods. Visuals is tool-only; do not
   advertise `resources/list`, `resources/read`, or resource templates.
3. Malformed nested `visual_manage.arguments` produces one corrective error.
4. Repeated identical protocol failures stop or escalate instead of looping.
5. Accumulated token/cost risk remains visible while a turn is looping.

Partial source coverage already exists:

- `apps/synth_desktop/tests/v02_mcp_schema.test.mjs`
- `apps/synth_desktop/skills/use-synth-visuals/SKILL.md`
- `apps/synth_desktop/skills/author-synth-diagrams/SKILL.md`
- `apps/synth_desktop/src-tauri/src/bin/synth_visuals_mcp.rs`

Do not assume that means G1 is complete. In particular, verify the identical
failure circuit breaker, shell behavior, and visible usage accounting in a
real agent task. Preserve the task journal and add a secret-free receipt.

### G2 — split and land trees (`SYN-3202`)

Never use `git add -A`. Review and commit each lane separately, then push/open
the intended PRs. CI is suspended/deploy-only, so local validation is required.

Current Workshop dirty paths at this handoff:

```text
apps/synth_desktop/src-tauri/Cargo.lock
apps/synth_desktop/src-tauri/Cargo.toml
apps/synth_desktop/src-tauri/src/optimizers/manager.rs
apps/synth_desktop/src/renderer/src/generated/protocol.ts
docs/launch/README.md
docs/launch/v0.2-e2e-suite.md
docs/launch/v0.2-launch.md
visuals/templates/optimizer.run.v1/examples/gepa_events.json
docs/launch/v0.3-themes.md (untracked)
```

Known classifications:

- `Cargo.toml` / `Cargo.lock` / `optimizers/manager.rs`: optimizer supervisor
  process-group drain work. It adds Unix process groups and a shutdown test.
  Validate it independently before committing.
- `generated/protocol.ts`: currently removes recent OAuth, permissions,
  optimizer, and renderer types. Treat this as suspect/stale generated output.
  Run the canonical protocol generation/conformance flow and commit only the
  generator-consistent result; do not accept this diff by inspection.
- `gepa_events.json`: optimizer fixture lane; keep with its matching projection
  or test change.
- `docs/launch/*` and `v0.3-themes.md`: launch-planning lane. Reconcile stale
  W3 quota language with the installed PASS receipt. Keep v0.3 explicitly
  parked until v0.2 GO.

Other repository/worktree state:

- Containers: `/Users/joshuapurtell/Documents/GitHub/containers`, branch
  `josh/aug12-containers-platform`, clean at `44443b4`. It is not yet an
  upstream release tip.
- GEPA: `/Users/joshuapurtell/Documents/GitHub/optimizers-g1`, branch
  `josh/aug12-g1-gepa`, tip `7f6f000`, with eight modified Rust files. Preserve
  the proven index-lock/timeout work and split unrelated platform changes.
- SFT: `/Users/joshuapurtell/Documents/GitHub/optimizers-beta-sft`, branch
  `josh/aug12-sft`, tip `11238d1`, with modified scripts plus untracked
  acceptance/curation helpers. Keep the hosted occupancy receipt and source
  changes together; omit disposable output.
- Do not land from dirty `/Users/joshuapurtell/Documents/GitHub/optimizers`
  `main` or the MAPO `optimizers-beta` checkout by accident; both contain broad
  unrelated work.
- `agent/aug12-modern-stack-completion` at `bd452e4` is a separate dirty
  Workshop worktree. Reconcile intentionally; do not copy its whole tree.

G2 is complete only when the chosen release tip is clean and every artifact
source SHA is recorded.

### G3 — fast E2E on the landed tip

Run from the clean Workshop release tip:

```bash
./scripts/v02-e2e-gates.sh
./scripts/desktop.sh conform
npm run desktop:check
```

Then use an isolated named Desktop instance, never the production profile, for
exactly four live checks:

1. Tool-loop: the G1 live receipt.
2. Approval: Ask/untrusted; one shell command; card stays above Working with
   Reject / Approve once; stopping clears both.
3. Visual honesty: reopen Craftax and, if local, Harbor evidence. Missing
   reward/usage is `—`, `stream.subscribed` is not evidence, and two visuals do
   not mix.
4. Visual-first smoke: find/register Craftax, bind the declared stream, then
   exactly one rollout; no guessed `/events`.

Use an independent tester for the final live pass. Record Workshop and
Containers SHAs, app version, instance name, and receipt paths. Do not turn the
dig.bench fixture shell into an A8 claim.

### G4 — friends-class artifact and GO (`SYN-3212`)

1. Start from the clean G2/G3 tip. The install script must refuse a dirty tree.
2. Build/install the actual distributable candidate with isolated data roots.
3. Sign it. Friends-class may use Open Anyway; public distribution must meet
   the required signing/notarization policy.
4. Record app executable SHA-256, bundle identifier, version, signing identity,
   CDHash, source SHAs, Containers pin, frontend hash, and PROVENANCE.
5. If the final runtime SHA drifted from the installed acceptance candidate,
   rerun the one-rollout visual-first smoke. Run ten only if the A1/runtime
   identity no longer applies.
6. Make a deliberate GO decision, then and only then flip public stable to
   `0.2.0`.

The prior installed candidate is evidence, not the final public artifact:

- Instance: `v02golden`
- Source: `859110f6a316f282445f6a6f71cad07df4fc8833`
- Executable SHA-256:
  `b44ad5f93bdd8a191c2556895249c2f287d3681bb0784627027ff1139b733cf7`
- Bundle ID: `com.synth.desktop.v02.dev.v02golden`
- Signature: ad hoc; CDHash `9e754e1b4225d829064ae8927284c10a2645f54c`

## Deferred / explicitly out of scope

- Public dig.bench/A8 — deferred by product decision. Do not block GO.
- Local OrbStack A2 reproduction and external Harbor Docker build-context fix.
- Manual VoiceOver/TCC acceptance.
- Visual-family modularization, optimizer sidecar packaging, Specta cutover,
  Whisper pinning, GELO cache repair, nested redactor consolidation.
- Intern (v0.4) and all v0.3 collaboration themes.
- Full extended Cargo/Playwright/Containers matrices beyond the defined GO
  gates, unless the landed changes make them relevant.

## Hard no-go conditions

- Dirty tree used as artifact source.
- Guessed `/events` or an invented stream slot/URL.
- Missing reward or cost coerced to zero.
- Credential/token in a receipt, screenshot, command line, or trace.
- Public dig.bench claimed from fixtures or mocks.
- Independent final reviewer is the implementer.
- Repeated identical tool failures can burn unbounded tokens.
- Public stable flipped before provenance, hashes, and deliberate GO.

## Environment cleanup already done

The `v02golden` app, W3 proxy, Craftax façade, and raw Rust engine were stopped.
Ports `18097`, `8298`, and `18098` were clear at handoff. The W3 fault state
was restored to `{}`. Do not assume any of those services are still running.

## Suggested first hour

1. Read this file and the `v0.2 GO scope` only; ignore stale earlier scoreboards.
2. Preserve the current dirty trees with read-only inspection; assign ownership
   of each lane before editing.
3. Finish/verify G1 and its cheap live receipt.
4. Regenerate protocol types and decide whether the current generated diff is
   discarded or replaced.
5. Split the Workshop commits, then GEPA/SFT, and construct the clean release
   tip before running G3.

The release is finished when G1–G4 are green on one recorded clean tip. No
additional W1–W3 or dig.bench work is required for v0.2.
