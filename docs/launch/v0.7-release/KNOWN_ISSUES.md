# v0.7 known issues (living)

Seeded 2026-08-20 from the v0.7 release work register (§0, §2, §3, §4b, §7) and its keeper log. Each entry is an **observed fact**, the register id, and the owner lane. An entry is removed only when a merged PR closes it; closed entries move to the bottom with the closing SHA.

## Open

| # | Observed fact | Register id | Owner |
|---|---|---|---|
| K1 | No merged code path selects a retained finetune/LoRA by id and launches inference or an Eval recipe with that id retained in the downstream receipt. `chat_checkpoint` posts to `/v1/chat/completions` with no adapter/snapshot argument (ambient "latest"); Eval recipes take policy-file candidate sets. PRs #45 (managed artifacts) and #46 (artifact inference) are open. | P0-1, P0-2, D6 | dev agent (flow) |
| K2 | Workshop does not provision the Eval runtime. `EVAL` contract is `official: "unmanaged"`, `min_supported: "0"`, `provisioned_by_desktop: false` (`src-tauri/src/contract/runtimes.rs:175-205`); the interpreter resolves from a developer `.venv` or `<state root>/optimizers/eval.toml`. The 0.2.14/0.2.15 pins are the **GEPA sidecar's**. Every prior release's "optimizer runtime ready" line (e.g. v0.4 ACCEPTANCE) was the sidecar, never Eval. PR #49 open. | P0-3b | L6 |
| K3 | `ghcr.io/synth-laboratories/workshop-craftax-eval-target` digest `02b076f8…` is pushed but the CI "make package public" step 404'd twice (runs 32094428767 + retry); not anonymously pullable, so `eval doctor` refuses both Craftax recipes on a fresh home. L5 probe 2026-08-20: `gh api /orgs/synth-laboratories/packages/container/workshop-craftax-eval-target` → 403 (token lacks `read:packages`) — visibility could not be confirmed either way from this lane. | P0-4 | L1 |
| K4 | `ghcr.io/synth-laboratories/workshop-gsm8k-eval-target` does not exist (API 404), so `eval.mlx.local-policy.smoke.v1` is `unavailable`; `fixture`/`gamebench` recipes have no published digest by design. | P1-9 | L2 |
| K5 | `openai/gsm8k` is loaded with no `revision=` (containers `runtimes/gsm8k_world.py:415`); no dataset pin or split digests exist in any repo. GSM8K has never been exercised through the loopback rollout lane. | P1-8, P1-10 | L2 |
| K6 | Local CISPO recipe is Banking77-only with the task hardcoded and rollout URL / warm start from `SYNTH_MLX_CISPO_ROLLOUT_URL` / `SYNTH_MLX_CISPO_WARM_START` env (`optimizers/cispo.rs:23,29,84-96`); violates the no-env-var-config rule. PR #51 ("pin local MLX to GSM8K and refuse dropout resume") is open. | P1-10, P1-14 | dev agent / L2 |
| K7 | Training event adapter: hosted SFT synthesizes `sequence` from `sequence_number` and wraps the raw payload; the MLX loop silently skips sequence gaps; MLX "succeeded" maps straight to `optimizer.run.completed` before artifact materialization. PR #48 ("persist training facts as optimizer_event.v1") is open. Envelope-name question (`training.event.v1` vs persisted `optimizer_event.v1`, register §4) is answered by #48's title only — confirm with Josh. | P0-6, §4 | dev agent |
| K8 | Hosted lane is not deployed: prod backend `/version` = `128588f` (v0.6); prod optimizers-beta answers `/v1/training/capabilities` 404 and `/v1/runtime-identity` 404 (pre-CISPO binary). Every hosted receipt so far ran on slot3 with a source-built binary and six hand-patched container artifacts (`~/migration/slot3-patches/`), which is not a release receipt. | P0-9, §4b rung 1, D2 | L1 (mechanism), L3 (rung 1), Josh (D2) |
| K9 | Hosted CISPO is `not_validated` (catalog rev `2026-08-19.v4`) with a stale `block_reason` citing the zero-variance canary whose cause (containers temperature bug) is fixed in containers `9ed2597`. The only hosted training receipt (`docs/launch/v0.7-cispo-healthbench-canary.md`) used Tinker native `importance_sampling`, not `cispo.slime.v1`, and failed the teacher-regression gate. Slot admission is the `SYNTH_DEV_SLOT_MANAGED` + `SYNTH_HOSTED_TRAINING_ALLOW_NOT_VALIDATED` env gate, not an authenticated validation-only path. | P1-16, D3 | L3 |
| K10 | CISPO identity has never been measured against Tinker: `run_hosted_tinker_rl.py:343` refuses other objectives but `verify_cispo_parity.py` has not run (one paid call). | P0-10, D4 | L3 (staged), Josh (spend) |
| K11 | Banking77 is saturated for gpt-oss-20b: CISPO 32/32 groups tied (`zero_advantage`), and SFT degraded it 0.81 → 0.47. Banking77 receipts are mechanism receipts, not uplift. | D1 | Josh |
| K12 | `synth_gepa` `service_ownership::tests::drop_does_not_delete_heartbeat_owned_by_another_pid` flakes under parallel execution: **4/30 runs fail on `d3c9edd` (runs 1, 4, 10, 25), 0/10 solo**. Mechanism: the heartbeat-writer thread rewrites the file with the real PID every 2s; the test overwrites with `pid=1` and drops immediately, so a write landing in between makes `Drop` see its own PID and delete the file. One-line fix: call `stop_heartbeat_writer()` before the overwrite (as the sibling test does). Keeper log says a fix PR is in flight; no such PR was open on `synth-laboratories/optimizers` at 2026-08-20 17:30Z. Use `--test-threads=1` meanwhile. | keeper log 15:25 / 16:10 | optimizers owner |
| K13 | Last packaged Craftax GEPA (0.2.14 sidecar) was 0-for-5 with `codex app-server timed out waiting for response` / `could not resolve native Codex binary`; Banking77 GEPA 1-of-2. Must be re-checked on the 0.2.15 sidecar and root-caused in the sidecar/app-server lane, not the reducer. | P1-23 | L7 |
| K14 | The shipped GEPA Banking77 recipe still injects `BANKING77_POLICY_CONCURRENCY=4` via env (`optimizers/recipes.rs:1944` at `701b483e`; register cited `:1765`). Minibatch-pool guard is present (`recipes.rs:2029-2041`); confirm `validate_limits` refuses pool `<= minibatch_size` on the packaged build. | P1-29 | L7 |
| K15 | v0.6 shipped with **zero CUA gates closed**; wrap failures W02 (signed-out state not preserved / Laguna 503) and W05 (stop/resume) were never re-verified. | P1-7 | L4 |
| K16 | CUA harness is container-rollout-shaped, not journey-shaped; Playwright specs are fixture-fed (`window.synthOptimizers` fakes); triage classes are `product/infra/harness/security`, not the plan's five; no harness-level budget/provider gate; the 37 manual items encode v0.1 claims. | P1-1..P1-4, P1-6 | L4 |
| K17 | None of the nine typed agent capabilities exist; the agent surface is `optimizer_manage` with recipe enums. Invariant to keep: nothing downloads silently (MLX child runs `HF_HUB_OFFLINE=1`). PR #52 open. | P1-13 | dev agent |
| K18 | Tinker catalog doc lists only Nemotron ids; context length for gpt-oss-20b disagrees (SDK probe 32K vs backend catalog 131072). No hosted checkpoint sampling/inference route exists. | P1-15, P1-18 | L3 |
| K19 | `scripts/desktop-instance.sh` on `v0.7` hard-fails for `RELEASE_LINE != v0.7` (`:19-25`) and pins `APP_VERSION=0.7.0`; a v0.6 instance cannot be built from this branch (by design — the v0.6 trap, below, was the mirror). `-dirty` source disqualifies `assert-identity`. | P0-12 | L1 |
| K20 | MAPO proposer checkpoint `37caf2b` (1,351 lines) sits on `origin/archive/v0.7-mapo-checkpoint-20260820` with no owner; DungeonGrid `origin/main` is an orphan root that must be reconciled before anything DungeonGrid is pushed. | §7 | unassigned |
| K21 | Dead code from the checkpoint land: `spawn_hosted_worker`/`run_hosted_worker` (`hosted_sft.rs`), unused `download_model` in `laguna.rs`; 107-warning Rust backlog. | §7 | dev agent |
| K22 | `codex/hosted-tinker-training` `569dc7b5` (parallel hosted model/checkpoint UX) is **not** on `v0.7`; left on its origin branch for a later replay (see `COMMIT_MAP.md` §D11). | D11 | dev agent |
| K23 | Stale worktrees `workshop-cua` (v0.5/cua, 206 behind; 2-file diff to salvage) and `workshop-computer-use` (282 behind) still exist. | §7 | L1 |
| K24 | Distribution is ad-hoc signed and unnotarized (D8 default, as v0.6.0); Gatekeeper rejects by default and the user must Control-click → Open. The Computer Use helper needs a Developer ID to ship at all. | D8 | Josh |
| K25 | Experiment layer is merged (optimizers PR #44 `279eaf5`) but not in the 0.2.15 wheel and has no Workshop surface; the `experiment.overview.v1` producer ingests no spec/`labels_json` and has no arm/block/claim-verdict view or drill-through to V5. | P1-19, P1-22, D9 | experiment-layer owner |
| K26 | Jesterky V5 workflow is disabled (`JesterkyWorkflowConfig` default off); its V5 bundle adapter and off/on ablation are unmerged (`origin/codex/eval-v5-annotation-policy`). | D10 | deferred |
| K27 | Fresh-worktree builds fail the Tauri resource check unless `scripts/stage-packaged-cookbooks.sh` has run and `helpers/synth-computer-use/target/bundle/Synth Computer Use.app` exists (`tauri.conf.json` `bundle.resources`). Documented in `docs/launch/README.md`. | §7 | L5 (docs), every lane (practice) |
| K28 | `docs/launch/v0.7-optimizers-runtime.md` "Current shortcuts" table described the pre-#42 tree; updated 2026-08-20 by L5. Hosted SFT still resolves its service URL from `SYNTH_OPTIMIZERS_SFT_SERVICE_URL` (default `http://127.0.0.1:8878`) inside `sidecar_training.rs:1100`; hosted GELO is still dispatched from the host (`service.rs:304,348`). | §7, P3/P5 | dev agent |

## Closed (retained for history)

| # | Fact | Closed by |
|---|---|---|
| C1 | `desktop-instance.sh` hard-failed on `RELEASE_LINE != v0.6` and pinned `APP_VERSION=0.6.0`, so no v0.7 instance could be built ("the v0.6 trap"). | workshop PR #47 `c589ea9a` (`2e64d4c3`): v0.7 line, `instances/v07/`, 0.2.15 sidecar pin |
| C2 | No `0.2.15` existed anywhere; PyPI latest was 0.2.14. | optimizers PR #42 `d3c9edd`, tag `v0.2.15`, wheel on PyPI 2026-08-20 16:09Z |
| C3 | Workshop `OPTIMIZERS_CONTRACT.official` / `min_supported` were 0.2.14. | workshop PR #47 (`runtimes.rs:142,153` → 0.2.15) |
| C4 | Training-sidecar work existed only as local checkpoint `4694270b`; model root split (`~/.synth-desktop/models/training` vs `instance::state_root()`). | workshop PR #42 `6fcb942e` |
| C5 | synth-mlx-rl `v0.7/real-backend-only` (12 commits) was unpushed; main checkout held UU/AA entries. | synth-mlx-rl PR #2 `23ee7c3` |
| C6 | evals CUA lanes `64366f246` / `2ab5a0891` were on no remote. | evals PR #280 → `dev` `ee80a748` |
| C7 | backend `origin/staging` carried 3 commits not on `v0.7`; promoting would have dropped them. | backend PR #1244 `769fba7e3` (one Alembic head; 58/58 slice) |
| C8 | PPO · GLM card and selector still rendered; SFT/CISPO sat in the optimizer search grid. | workshop PR #47 (P0-7) |
| C9 | `release/v0.6-captain-cua` commits `7dbcca7e` / `02d633d8` were not on `v0.7`. | cherry-picked in PR #42 (`0a7ce836`, `94c90710`) |
| C10 | Hosted CISPO launch did not bind to the retained SFT artifact. | workshop PR #50 `701b483e` |
