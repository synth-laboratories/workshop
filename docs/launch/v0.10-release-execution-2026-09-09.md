# v0.10 release execution — 2026-09-09

Status: **not released**. The user requested completion of the release, including the previously planned publication and deployment work. Earlier handoff holds are no longer the authorization blocker; source reconciliation, release gates, and credential availability remain binding.

## Verified public state

- Workshop candidate `0f15f76d` was pushed to `origin/codex/v010-complete` using SSH with `UseKeychain=no` and `BatchMode=yes`.
- Workshop `origin/dev`: `4813960d93fc5dfa816ffcd7f0917702792df989`.
- Workshop `origin/main`: `ab4de97a5d7e13046cd594c160a45628794e343b`.
- No remote `v0.10*` tag was returned by `git ls-remote`.
- Public `/releases/stable/latest.json` still reports v0.9.3, source `fd9b5bb30ee66b91a8ca1ccd94a91404012e57bd`, ad-hoc signing, no notarization, artifact SHA-256 `6073029fde9ba4c470d5956bf4b6c5d8b26eaa5d81fbf65485899f34b18fe7cd`.
- Staging `/version` reports backend `6cd19f2310b0c1f2d508bc6c80987f910274b3d4`.
- Production `/version` reports backend `44983cddc3b30a5f801f462097ef5b965a8f2985`.
- Prepared backend staging candidate remains `bf9cee998`; prepared frontend staging candidate remains `24ba38667d770df5f4abb3ef43f0218807e25280`. Neither was promoted by this execution.

## Verification and corrections

Fresh `npm run typecheck`, `npm run build:graph`, `npm run test:visuals`, and `npm run test:a11y` passed. Accessibility: 722 passed, zero failures or skips. The graph build compiled 625 modules; the existing Reports static/dynamic import warning remains.

The candidate omitted the public branch's `.github/workflows/desktop-conform.yml`. Restored its conformance and CSS checks, with push triggers on `dev` and `main`. Restored the omitted item-31 conformance counters from public `main`, adapting the visual package scan to `packages/workshop-visuals` so symlinks do not hide source. `./scripts/desktop.sh conform`, CSS literal lint, and `git diff --check` pass as commands. The counters are diagnostics, not evidence that architectural debt is zero: sequence fold 18, gap construction 1, table creation 2, glob discovery 9, template IDs 12.

The full native library suite was started with one test thread; its result must be recorded before this gate is closed.

## Merge blockers discovered

`git merge-tree --write-tree HEAD origin/dev` resolves textually. The equivalent comparison against `origin/main` reports extensive conflicts, including renamed visuals paths. Public v0.8 history was reconstructed, so a wholesale resolution favoring either side is unsafe.

Production's activation changes are represented in candidate commit `4ed87e47`, but this does not establish full production parity. In particular, production telemetry includes `consent.rs`, `contract.rs`, `flush.rs`, `policy.rs`, `sink_http.rs`, and `store.rs`; the candidate removes those modules and returns to the dictionary/local-store implementation. Reconcile the consent UI, event contract, persistence, and backend sync behavior before promotion. Do not erase this difference with an ancestry-only merge.

## Access blockers

- GitHub connector PR creation returned HTTP 403, `Resource not accessible by integration`. No PR was created.
- The in-app GitHub browser is signed out.
- GitHub CLI reports a Keychain-backed login. It was not used for PR creation or publishing; no credential was extracted.
- Environment-variable name checks found no release GitHub token, Railway token, or Apple signing/notarization credential in the current process or the inspected project-local env files. Product OAuth/GitHub App credentials were not repurposed as release credentials.
- The helper's current notarization path uses `--keychain-profile`. It cannot be run under the user's current credential restriction without specific authorization or an authorized alternative signing/notarization mechanism.

## Next execution boundary

Complete production-source reconciliation and native verification, then use a GitHub identity authorized to create and merge release PRs. Follow the [publication checklist](v0.10-publication-and-production-checklist.md) for the stable package train, reviewed billing migration, staging verification, production deployment, and final signed artifact. No package, production branch, deployment, or public download metadata was changed by this execution.
