# annotation.overlay.v1

Markers on a **sealed Trace V5**. Annotations are an overlay evidence layer — they never mutate the sealed trace.

## Slots

| Slot | Notes |
| --- | --- |
| `trace` | digest / catalog id (read-only) |
| `annotations` | marker list |

## Kinds

`note` · `bug` · `highlight` · `reward` · `acceptance`
