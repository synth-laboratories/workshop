id = "nanohorizon.craftax.glm-5.3-flash.eval.v1"
algorithm = "eval"
title = "NanoHorizon Craftax · GLM 5.3 Flash"
container = "nanohorizon-craftax"
provider = "openrouter"
model = "z-ai/glm-5.3-flash"
locality = "container"
credential_mode = "proxy"
family = "craftax"
harness = "nanohorizon"
policy_config = "glm-5.3-flash"
policy_source = "src/challenge/policy.py"
requires_credential_advertisement = false
concurrency = 5
train_seeds = [780000, 780001, 780002, 780003, 780004]
heldout_seeds = []

[bounds]
max_cost_usd = 2.45
max_total_rollouts = 5

[policy]
api = "chat_completions"
effort = "medium"
max_calls = 10
max_steps = 2000
compact_after_tokens = 10000
max_compactions = 20
# Canonical GLM 5.3 Flash profile value. Keep this manifest as the only
# Workshop recipe declaration; tests assert the public v0.9.1 seed ledger.
thinking_budget = 640
answer_max_tokens = 256
context_token_budget = 32000
min_actions = 2
max_actions = 10
timeout_seconds = 180.0
min_request_interval = 1.0
sampler_retries = 12
retry_max_wait = 60.0
