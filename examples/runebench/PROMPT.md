Help me try this checkout's `examples/runebench` example. Read its README and
manifest, then do the setup for me without modifying existing projects.

Start with the bundled recorded sample when available. Open it as a native
Workshop visual using the advertised visual tools, the generated
`visual-request.json`, and this checkout's visual skill. Inspect the rendered
view before saying it works. If native visual tools are unavailable, say so
explicitly; the browser viewer is a separate fallback, not native acceptance.

Show all four agents, focus one, compare two, inspect a message and an unsuccessful
action if one was recorded, then follow an exact source record and game frame.
Do not invent a failure or message if this sample lacks one.

Next run `bash examples/runebench/example.sh run` for a fresh 25-second scripted
episode. Check Docker first and ask before installing a system application.
This run needs no AI credentials. Rebuild the review, refresh the same visual,
and explain the recorded outcome with three exact actor/event citations.

Do not run any paid model policies or silently substitute an API provider. After
the scripted path works, explain the optional OpenRouter mode and its enforced
aggregate budget, then wait for my approval. My ChatGPT login does not authorize
paid game agents. Never read Keychain or paste a key into a tool argument or chat.
